[project]
name = "python-sdk-health-mcp"
version = "0.1.0"
description = "MCP server for Python SDK health"
requires-python = ">=3.10"
license = "MIT"
readme = "README.md"
authors = [
    {name = "Azure SDK Team", email = "azpysdk@microsoft.com"},
]
dependencies = [
    "mcp[cli]>=1.6.0",
    "httpx>=0.24.0",
]


