parameters:
  - name: ServiceDirectory
    type: string
    default: not-specified
  - name: SkipPrValidation
    type: boolean
    default: false
  - name: Artifacts
    type: object
    default: []
  - name: TestPipeline
    type: boolean
    default: false
  - name: BeforeTestSteps
    type: object
    default: []
  - name: AfterTestSteps
    type: object
    default: []
  - name: ExcludePaths
    type: object
    default: []
  - name: BeforePublishSteps
    type: object
    default: []
  - name: TestMarkArgument
    type: string
    default: ''
  - name: BuildTargetingString
    type: string
    default: azure-*
  - name: TestTimeoutInMinutes
    type: number
    default: 60
  - name: InjectedPackages
    type: string
    default: ''
  - name: BuildDocs
    type: boolean
    default: true
  - name: DevFeedName
    type: string
    default: public/azure-sdk-for-python
  - name: PublicFeed
    type: string
    default: 'PyPi'
  - name: PublicPublishEnvironment
    type: string
    default: package-publish
  - name: TargetDocRepoOwner
    type: string
    default: MicrosoftDocs
  - name: TargetDocRepoName
    type: string
    default: azure-docs-sdk-python
  - name: MatrixConfigs
    type: object
    default:
      - Name: Python_ci_test_base
        Path: eng/pipelines/templates/stages/platform-matrix.json
        Selection: sparse
        GenerateVMJobs: true
  - name: AdditionalMatrixConfigs
    type: object
    default: []
  - name: MatrixFilters
    type: object
    default: []
  - name: MatrixReplace
    type: object
    default: []
  - name: VerifyAutorest
    type: boolean
    default: false
  - name: TestProxy
    type: boolean
    default: true
  - name: GenerateApiReviewForManualOnly
    type: boolean
    default: false
  - name: oneESTemplateTag
    type: string
    default: release
  - name: EnableCompiledCodeql
    type: boolean
    default: false
  - name: EnvVars
    type: object
    default: {}

extends:
  template: /eng/pipelines/templates/stages/1es-redirect.yml
  parameters:
    oneESTemplateTag: ${{ parameters.oneESTemplateTag }}
    EnableCompiledCodeql: ${{ parameters.EnableCompiledCodeql }}
    stages:
      - ${{ if and(eq(parameters.SkipPrValidation, true), eq(variables['Build.Reason'], 'Manual')) }}:
        - stage: NoOp
          displayName: No-op
          variables:
            - template: /eng/pipelines/templates/variables/globals.yml
            - template: /eng/pipelines/templates/variables/image.yml
          jobs:
            - job: NoOp
              displayName: No-op
              pool:
                name: $(LINUXPOOL)
                image: $(LINUXVMIMAGE)
                os: linux
              steps:
                - checkout: none
                - pwsh: Write-Host "PR validation skipped because SkipPrValidation was set to true for a manual run."
                  displayName: Skip PR validation

      - stage: Build
        condition: not(and(eq(${{ parameters.SkipPrValidation }}, true), eq(variables['Build.Reason'], 'Manual')))
        jobs:
          - template: /eng/pipelines/templates/jobs/ci.yml
            parameters:
              ServiceDirectory: ${{ parameters.ServiceDirectory }}
              Artifacts: ${{ parameters.Artifacts }}
              ${{ if eq(parameters.ServiceDirectory, 'template') }}:
                TestPipeline: true
              BeforeTestSteps: ${{ parameters.BeforeTestSteps }}
              AfterTestSteps: ${{ parameters.AfterTestSteps }}
              ExcludePaths: ${{ parameters.ExcludePaths }}
              BeforePublishSteps: ${{ parameters.BeforePublishSteps }}
              TestMarkArgument: ${{ parameters.TestMarkArgument }}
              BuildTargetingString: ${{ parameters.BuildTargetingString }}
              TestTimeoutInMinutes: ${{ parameters.TestTimeoutInMinutes }}
              InjectedPackages: ${{ parameters.InjectedPackages }}
              BuildDocs: ${{ parameters.BuildDocs }}
              DevFeedName: ${{ parameters.DevFeedName }}
              EnvVars: ${{ parameters.EnvVars }}
              MatrixConfigs:
                - ${{ each config in parameters.MatrixConfigs }}:
                    - ${{ config }}
                - ${{ each config in parameters.AdditionalMatrixConfigs }}:
                    - ${{ config }}
              MatrixFilters: ${{ parameters.MatrixFilters }}
              MatrixReplace: ${{ parameters.MatrixReplace }}
              VerifyAutorest: ${{ parameters.VerifyAutorest }}
              TestProxy: ${{ parameters.TestProxy }}
              GenerateApiReviewForManualOnly: ${{ parameters.GenerateApiReviewForManualOnly }}

          - ${{ if and(eq(variables['System.TeamProject'], 'internal'), or(in(variables['Build.Reason'], 'Manual', ''), and(eq(variables['Build.Reason'], 'IndividualCI'), eq(variables['Build.SourceBranch'], 'refs/heads/main')))) }}:
              - job: FilterPackageInfo
                displayName: Filter PackageInfo files for API review
                dependsOn:
                  - Build_Extended
                  - Analyze
                condition: >-
                  and(
                    in(dependencies.Build_Extended.result, 'Succeeded', 'SucceededWithIssues'),
                    or(
                      in(dependencies.Analyze.result, 'Succeeded', 'SucceededWithIssues'),
                      and(
                        eq(dependencies.Analyze.result, 'Skipped'),
                        eq(variables['Skip.Analyze'], 'true')
                      )
                    )
                  )
                pool:
                  name: $(LINUXPOOL)
                  image: $(LINUXVMIMAGE)
                  os: linux
                steps:
                  - checkout: none

                  - download: current
                    artifact: packages_extended

                  - pwsh: |
                      $packageInfoFiles = Get-ChildItem -Path '$(Pipeline.Workspace)/packages_extended/PackageInfo' -Filter '*.json' -File |
                        Where-Object {
                          $packageInfo = Get-Content -Raw -Path $_.FullName | ConvertFrom-Json
                          if ($packageInfo.SdkType -eq 'mgmt') {
                            Write-Host "API Review is not required for management plane SDKs: $($packageInfo.Name)"
                            return $false
                          }
                          return $true
                        } |
                        ForEach-Object { "packages_extended/PackageInfo/$($_.Name)" }
                      $hasApiReviewPackages = $packageInfoFiles.Count -gt 0
                      Write-Host "##vso[task.setvariable variable=ApiReviewPackageInfoFiles;isOutput=true]$($packageInfoFiles -join "','")"
                      Write-Host "##vso[task.setvariable variable=HasApiReviewPackages;isOutput=true]$hasApiReviewPackages"
                    name: filterPackageInfo
                    displayName: Filter management plane packages

              - job: CheckPackageApproval
                displayName: Check Package Approval Status
                dependsOn: FilterPackageInfo
                condition: >-
                  and(
                    in(dependencies.FilterPackageInfo.result, 'Succeeded', 'SucceededWithIssues'),
                    eq(dependencies.FilterPackageInfo.outputs['filterPackageInfo.HasApiReviewPackages'], 'True')
                  )
                variables:
                  ApiReviewPackageInfoFiles: $[ dependencies.FilterPackageInfo.outputs['filterPackageInfo.ApiReviewPackageInfoFiles'] ]
                pool:
                  name: $(LINUXPOOL)
                  image: $(LINUXVMIMAGE)
                  os: linux
                steps:
                  - checkout: self
                    fetchDepth: 1
                    fetchTags: false
                    sparseCheckoutDirectories: |
                      eng/common
                      eng/scripts

                  - download: current
                    artifact: packages_extended

                  - template: /eng/common/pipelines/templates/steps/install-azsdk-cli.yml

                  - template: /eng/common/pipelines/templates/steps/get-package-approval-status.yml
                    parameters:
                      PackageInfoFiles:
                        - $(ApiReviewPackageInfoFiles)
                      RepoOwner: Azure

        variables:
          - template: /eng/pipelines/templates/variables/globals.yml
          - template: /eng/pipelines/templates/variables/image.yml
          - template: /eng/common/pipelines/templates/variables/api-review-break-glass.yml

      - template: archetype-python-release.yml
        parameters:
          DependsOn: "Build"
          ServiceDirectory: ${{ parameters.ServiceDirectory }}
          Artifacts: ${{ parameters.Artifacts }}
          ${{ if eq(parameters.ServiceDirectory, 'template') }}:
            TestPipeline: true
          ArtifactName: packages_extended
          DocArtifact: documentation
          TargetDocRepoOwner: ${{ parameters.TargetDocRepoOwner }}
          TargetDocRepoName: ${{ parameters.TargetDocRepoName }}
          DevFeedName: ${{ parameters.DevFeedName }}
          PublicFeed: ${{ parameters.PublicFeed }}
          PublicPublishEnvironment: ${{ parameters.PublicPublishEnvironment }}
