# this is a template tox.ini file used where there is no package-specific configuration
# note that this file will be copied into an appropriate package folder during build-CI. To locally leverage this, 
# do one of the following:
# 1) Execute your tests by using `python ./scripts/devops_tasks/setup_execute_tests.py <target-package>`
# 2) Manually copy this file into your package folder. `cd` to the folder. run `tox -e <environment>`

[tox]
envlist = {python2.7,python3.5,python3.4,python3.6,python3.7}-{linux,macos,windows}-{test},{python3.6,python2.7}-{lint},dev

[base]
deps = 
  -rdev_requirements.txt
  lint: pylint==2.3.0; python_version >= '3.4'
  lint: pylint==1.9.4; python_version < '3.4'

[pytest]
out_options = --junitxml={toxinidir}/test-junit-{envname}.xml --verbose --durations=10
cov_options = --cov --cov-report=
markers = cosmosEmulator: indicates a test that requires an active cosmos emulator to properly execute

[testenv]
pre-deps =
  wheel
skip_install = true
skipsdist = true
platform = linux: linux
           macos: darwin
           windows: win32
passenv = *
deps = {[base]deps}
changedir = {toxinidir}

commands = 
    test: python {toxinidir}/../../../eng/tox/create_wheel_and_install.py -d {distdir} -p {toxinidir}
    test: pytest \
    test:  {[pytest]cov_options} \
    test:  {[pytest]out_options} \
    test:  {posargs} \
    test:  {toxinidir}

    lint: pylint \
    lint:  --output-format=parseable \
    lint:  --rcfile {toxinidir}/../../../pylintrc \
    lint:  --exit-zero \
    lint:  {toxinidir}/azure

[testenv:dev]
usedevelop = True

commands =
    pytest \
      {[pytest]cov_options} \
      {[pytest]out_options} \
      {posargs} \
      {toxinidir}

    pylint \
      --output-format=parseable \
      --rcfile {toxinidir}/../../../pylintrc \
      --exit-zero \
      {toxinidir}/azure
