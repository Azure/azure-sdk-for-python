# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from msrest.service_client import SDKClient
from msrest import Configuration, Serializer, Deserializer
from .version import VERSION
from .operations.query_operations import QueryOperations
from . import models


class OperationalInsightsDataClientConfiguration(Configuration):
    """Configuration for OperationalInsightsDataClient
    Note that all parameters used to create this instance are saved as instance
    attributes.

    :param workspace_id: ID of the workspace. This is Workspace ID from the
     Properties blade in the Azure portal.
    :type workspace_id: str
    :param credentials: Subscription credentials which uniquely identify
     client subscription.
    :type credentials: None
    :param str base_url: Service URL
    """

    def __init__(
            self, workspace_id, credentials, base_url=None):

        if workspace_id is None:
            raise ValueError("Parameter 'workspace_id' must not be None.")
        if credentials is None:
            raise ValueError("Parameter 'credentials' must not be None.")
        if not base_url:
            base_url = 'https://api.loganalytics.io/v1'

        super(OperationalInsightsDataClientConfiguration, self).__init__(base_url)

        self.add_user_agent('azure-operationalinsights/{}'.format(VERSION))

        self.workspace_id = workspace_id
        self.credentials = credentials


class OperationalInsightsDataClient(SDKClient):
    """Operational Insights Data Client

    :ivar config: Configuration for client.
    :vartype config: OperationalInsightsDataClientConfiguration

    :ivar query: Query operations
    :vartype query: azure.operationalinsights.operations.QueryOperations

    :param workspace_id: ID of the workspace. This is Workspace ID from the
     Properties blade in the Azure portal.
    :type workspace_id: str
    :param credentials: Subscription credentials which uniquely identify
     client subscription.
    :type credentials: None
    :param str base_url: Service URL
    """

    def __init__(
            self, workspace_id, credentials, base_url=None):

        self.config = OperationalInsightsDataClientConfiguration(workspace_id, credentials, base_url)
        super(OperationalInsightsDataClient, self).__init__(self.config.credentials, self.config)

        client_models = {k: v for k, v in models.__dict__.items() if isinstance(v, type)}
        self.api_version = 'v1'
        self._serialize = Serializer(client_models)
        self._deserialize = Deserializer(client_models)

        self.query = QueryOperations(
            self._client, self.config, self._serialize, self._deserialize)
