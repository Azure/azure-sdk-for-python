{% set name = "azure-monitor-ingestion" %}
{% set version = "2023.06.01" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://files.pythonhosted.org/packages/8c/c8/404575e0f1882d2ace0a6eb62228ad18cd20ed0869848e50fc0c24025aed/azure-monitor-ingestion-1.0.1.zip"
  sha256: 27440dfd7b660412b551a3c110f963307bb8968c7d0a8d548a0543ff34d7558d

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - azure-core >=2023.06.01
    - azure-identity >=2023.06.01
    - pip
    - python
    - six
    - requests-oauthlib >=0.5.0
    - aiohttp
    - isodate
  run:
    - azure-core >=2023.06.01
    - azure-identity >=2023.06.01
    - python
    - six
    - requests-oauthlib >=0.5.0
    - aiohttp
    - isodate

test:
  imports:
    - azure.monitor.ingestion
    - azure.monitor.ingestion.aio

about:
  home: "https://github.com/Azure/azure-sdk-for-python/tree/main/sdk/monitor/azure-monitor-ingestion"
  license: MIT
  license_family: MIT
  license_file: 
  summary: "Microsoft Azure Monitor Ingestion client library for Python"
  description: |
    The Azure Monitor Ingestion client library is used to send custom logs to Azure Monitor using the Logs Ingestion API.
    This library allows you to send data from virtually any source to supported built-in tables or to custom tables that you create in Log Analytics workspace. You can     even extend the schema of built-in tables with custom columns.
    Please see https://aka.ms/azsdk/conda/releases/ingestion for version details.
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - xiangyan99
