trigger: none

extends:
  template: ./tests.yml
  parameters:
    TestMarkArgument: "automl_test"
    CloudConfig:
      Public:
        SubscriptionConfiguration: $(python-ml-automl-sub-scope)
        Location: eastus
