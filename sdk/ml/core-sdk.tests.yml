trigger: none

extends:
  template: ./tests.yml
  parameters:
    TestMarkArgument: "core_sdk_test"
    CloudConfig:
      Public:
        SubscriptionConfiguration: $(python-ml-core-sdk-sub-scope)
        Location: eastus2euap
        MatrixReplace:
          - TestSamples=.*/true