trigger: none

extends:
  template: ./tests.yml
  parameters:
    TestMarkArgument: "production_experiences_test"
    CloudConfig:
      Public:
        SubscriptionConfiguration: $(python-ml-experiences-sub-scope)
        Location: eastus
