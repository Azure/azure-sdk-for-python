[pytest]
# Standalone rootdir so the parent tests/conftest.py (which needs devtools_testutils) is NOT loaded.
# These smoke tests are pure-offline wire-serialization checks.
addopts = -q -p no:cacheprovider
python_files = test_*.py
python_classes = Test*
python_functions = test_*
