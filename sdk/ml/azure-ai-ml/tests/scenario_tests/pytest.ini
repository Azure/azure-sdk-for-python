[pytest]
# Isolate scenario tests from the parent tests/conftest.py which
# requires devtools_testutils (not needed for live scenario tests).
# Setting rootdir here prevents pytest from walking up to the parent conftest.
