name: echo-agent-conversations-websocket-streaming
description: >
  A simple echo agent that streams responses word-by-word using the
  azure-ai-agentserver-conversations SDK with WebSocket streaming.
metadata:
  tags:
    - AI Agent Hosting
    - Azure AI AgentServer
    - Conversations Protocol
    - Streaming
template:
  name: echo-agent-conversations-streaming
  kind: hosted
  protocols:
    - protocol: conversations
      version: v0.0.1
