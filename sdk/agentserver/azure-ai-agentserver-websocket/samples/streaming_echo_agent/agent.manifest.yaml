name: echo-agent-invocations-websocket-streaming
description: >
  A simple echo agent that streams responses word-by-word using the
  azure-ai-agentserver-invocations SDK with WebSocket streaming.
metadata:
  tags:
    - AI Agent Hosting
    - Azure AI AgentServer
    - Invocations Protocol
    - Streaming
template:
  name: echo-agent-invocations-streaming
  kind: hosted
  protocols:
    - protocol: invocations
      version: v0.0.1
