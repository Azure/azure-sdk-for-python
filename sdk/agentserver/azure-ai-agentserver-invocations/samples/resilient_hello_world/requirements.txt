# Deliberately minimal: this sample needs ONLY the two agentserver packages —
# no azure-ai-projects, no openai, no langgraph — so it runs with the smallest
# possible dependency and memory footprint.
#
# Constrained to compatible release lines (matching the package's own
# convention): the strictly-opt-in durable-task behaviour and local state store
# this sample relies on require Core 2.1.0b1+.
azure-ai-agentserver-core>=2.1.0b1,<3.0.0
azure-ai-agentserver-invocations>=1.2.0b1,<2.0.0
