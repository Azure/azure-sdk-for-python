# Exact known-failure node IDs for the complete tests/ run with Rust selected.
# Add only a reviewed tests/...py::Class::test_method[parameters] ID, preceded
# by a comment naming the verified limitation and its tracking decision.
# IDs are case-sensitive; wildcards and feature-name substring matching are
# not supported. A matching ID is an exemption, not evidence of parity.
#
# The previous broad feature names (including query and feed) were removed
# because they could conceal failures in already migrated behavior. No exact
# replacements have been justified by a reviewed full-suite run yet, so this
# list currently exempts nothing. Remove each exemption when its gap closes.
