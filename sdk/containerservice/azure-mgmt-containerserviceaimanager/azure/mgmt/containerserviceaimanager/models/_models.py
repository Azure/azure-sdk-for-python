# pylint: disable=line-too-long,useless-suppression,too-many-lines
# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
# Code generated by Microsoft (R) Python Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is regenerated.
# --------------------------------------------------------------------------
# pylint: disable=useless-super-delegation

import datetime
from typing import Any, Mapping, Optional, TYPE_CHECKING, Union, overload

from .._utils.model_base import Model as _Model, rest_field

if TYPE_CHECKING:
    from .. import models as _models


class Resource(_Model):
    """Resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    """

    id: Optional[str] = rest_field(visibility=["read"])
    """Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}."""
    name: Optional[str] = rest_field(visibility=["read"])
    """The name of the resource."""
    type: Optional[str] = rest_field(visibility=["read"])
    """The type of the resource. E.g. \"Microsoft.Compute/virtualMachines\" or
     \"Microsoft.Storage/storageAccounts\"."""
    system_data: Optional["_models.SystemData"] = rest_field(name="systemData", visibility=["read"])
    """Azure Resource Manager metadata containing createdBy and modifiedBy information."""


class TrackedResource(Resource):
    """Tracked Resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    :ivar location: The geo-location where the resource lives. Required.
    :vartype location: str
    """

    tags: Optional[dict[str, str]] = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """Resource tags."""
    location: str = rest_field(visibility=["read", "create"])
    """The geo-location where the resource lives. Required."""

    @overload
    def __init__(
        self,
        *,
        location: str,
        tags: Optional[dict[str, str]] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class AIManager(TrackedResource):
    """The AI Manager resource. For more information, see `https://aka.ms/aks/aimanager
    <https://aka.ms/aks/aimanager>`_.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    :ivar location: The geo-location where the resource lives. Required.
    :vartype location: str
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: ~azure.mgmt.containerserviceaimanager.models.AIManagerProperties
    :ivar e_tag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype e_tag: str
    :ivar identity: The managed service identities assigned to this resource.
    :vartype identity: ~azure.mgmt.containerserviceaimanager.models.ManagedServiceIdentity
    """

    properties: Optional["_models.AIManagerProperties"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The resource-specific properties for this resource."""
    e_tag: Optional[str] = rest_field(name="eTag", visibility=["read"])
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""
    identity: Optional["_models.ManagedServiceIdentity"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The managed service identities assigned to this resource."""

    @overload
    def __init__(
        self,
        *,
        location: str,
        tags: Optional[dict[str, str]] = None,
        properties: Optional["_models.AIManagerProperties"] = None,
        identity: Optional["_models.ManagedServiceIdentity"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ProxyResource(Resource):
    """Proxy Resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    """


class AIManagerNamespace(ProxyResource):
    """The AI Manager namespace resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: ~azure.mgmt.containerserviceaimanager.models.AIManagerNamespaceProperties
    :ivar e_tag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype e_tag: str
    """

    properties: Optional["_models.AIManagerNamespaceProperties"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The resource-specific properties for this resource."""
    e_tag: Optional[str] = rest_field(name="eTag", visibility=["read"])
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""

    @overload
    def __init__(
        self,
        *,
        properties: Optional["_models.AIManagerNamespaceProperties"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class AIManagerNamespaceProperties(_Model):
    """AI Manager namespace properties.

    :ivar provisioning_state: The status of the last operation. Known values are: "Succeeded",
     "Failed", "Canceled", "Creating", "Updating", and "Deleting".
    :vartype provisioning_state: str or
     ~azure.mgmt.containerserviceaimanager.models.AIManagerNamespaceProvisioningState
    :ivar labels: Labels applied to the Kubernetes namespace.
    :vartype labels: dict[str, str]
    :ivar annotations: Annotations applied to the Kubernetes namespace.
    :vartype annotations: dict[str, str]
    """

    provisioning_state: Optional[Union[str, "_models.AIManagerNamespaceProvisioningState"]] = rest_field(
        name="provisioningState", visibility=["read"]
    )
    """The status of the last operation. Known values are: \"Succeeded\", \"Failed\", \"Canceled\",
     \"Creating\", \"Updating\", and \"Deleting\"."""
    labels: Optional[dict[str, str]] = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """Labels applied to the Kubernetes namespace."""
    annotations: Optional[dict[str, str]] = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """Annotations applied to the Kubernetes namespace."""

    @overload
    def __init__(
        self,
        *,
        labels: Optional[dict[str, str]] = None,
        annotations: Optional[dict[str, str]] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class AIManagerPatch(_Model):
    """The AI Manager resource patch model.

    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    :ivar identity: The managed service identities assigned to this resource.
    :vartype identity: ~azure.mgmt.containerserviceaimanager.models.ManagedServiceIdentity
    """

    tags: Optional[dict[str, str]] = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """Resource tags."""
    identity: Optional["_models.ManagedServiceIdentity"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The managed service identities assigned to this resource."""

    @overload
    def __init__(
        self,
        *,
        tags: Optional[dict[str, str]] = None,
        identity: Optional["_models.ManagedServiceIdentity"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class AIManagerProperties(_Model):
    """AI Manager properties.

    :ivar provisioning_state: The status of the last operation. Known values are: "Succeeded",
     "Failed", "Canceled", "Creating", "Updating", and "Deleting".
    :vartype provisioning_state: str or
     ~azure.mgmt.containerserviceaimanager.models.AIManagerProvisioningState
    :ivar delete_policy: Delete options of the AI Manager. Defaults to ``Delete`` if not specified.
     Known values are: "Keep" and "Delete".
    :vartype delete_policy: str or ~azure.mgmt.containerserviceaimanager.models.DeletePolicy
    :ivar managed_resource_group_name: The name of the managed resource group created by the AI
     Manager to hold underlying infrastructure resources.
    :vartype managed_resource_group_name: str
    """

    provisioning_state: Optional[Union[str, "_models.AIManagerProvisioningState"]] = rest_field(
        name="provisioningState", visibility=["read"]
    )
    """The status of the last operation. Known values are: \"Succeeded\", \"Failed\", \"Canceled\",
     \"Creating\", \"Updating\", and \"Deleting\"."""
    delete_policy: Optional[Union[str, "_models.DeletePolicy"]] = rest_field(
        name="deletePolicy", visibility=["read", "create", "update", "delete", "query"]
    )
    """Delete options of the AI Manager. Defaults to ``Delete`` if not specified. Known values are:
     \"Keep\" and \"Delete\"."""
    managed_resource_group_name: Optional[str] = rest_field(name="managedResourceGroupName", visibility=["read"])
    """The name of the managed resource group created by the AI Manager to hold underlying
     infrastructure resources."""

    @overload
    def __init__(
        self,
        *,
        delete_policy: Optional[Union[str, "_models.DeletePolicy"]] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class AIModel(ProxyResource):
    """An AI model exposed by Microsoft.ContainerService. Read-only, globally-shared catalog entry
    that is platform-maintained and auto-provisioned by the resource provider. Can be referenced by
    ``ModelDeployment`` resources.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: ~azure.mgmt.containerserviceaimanager.models.AIModelProperties
    """

    properties: Optional["_models.AIModelProperties"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The resource-specific properties for this resource."""

    @overload
    def __init__(
        self,
        *,
        properties: Optional["_models.AIModelProperties"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class AIModelProperties(_Model):
    """AI model properties.

    :ivar model_id: The Hugging Face model identifier in ``<org>/<repo>`` form, e.g.
     ``microsoft/Phi-4-mini-instruct``. Uniquely identifies the upstream model that backs this
     catalog entry. Required.
    :vartype model_id: str
    :ivar description: An optional, free-form description of the model.
    :vartype description: str
    :ivar spec: Specification of the model. Required.
    :vartype spec: ~azure.mgmt.containerserviceaimanager.models.ModelSpec
    """

    model_id: str = rest_field(name="modelId", visibility=["read"])
    """The Hugging Face model identifier in ``<org>/<repo>`` form, e.g.
     ``microsoft/Phi-4-mini-instruct``. Uniquely identifies the upstream model that backs this
     catalog entry. Required."""
    description: Optional[str] = rest_field(visibility=["read"])
    """An optional, free-form description of the model."""
    spec: "_models.ModelSpec" = rest_field(visibility=["read"])
    """Specification of the model. Required."""


class AutoscaleProfile(_Model):
    """Autoscaling configuration: scale replica count between a minimum and maximum.

    :ivar min_replicas: The minimum number of replicas. Must be at least ``1``; scale-to-zero is
     not supported in autoscale mode (see ``ScalingProfile``). Required.
    :vartype min_replicas: int
    :ivar max_replicas: The maximum number of replicas. If not specified, the service derives a
     default from the subscription GPU quota.
    :vartype max_replicas: int
    """

    min_replicas: int = rest_field(name="minReplicas", visibility=["read", "create", "update", "delete", "query"])
    """The minimum number of replicas. Must be at least ``1``; scale-to-zero is not supported in
     autoscale mode (see ``ScalingProfile``). Required."""
    max_replicas: Optional[int] = rest_field(
        name="maxReplicas", visibility=["read", "create", "update", "delete", "query"]
    )
    """The maximum number of replicas. If not specified, the service derives a default from the
     subscription GPU quota."""

    @overload
    def __init__(
        self,
        *,
        min_replicas: int,
        max_replicas: Optional[int] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class CalculateCostPlan(_Model):
    """A GPU SKU pricing plan returned by the ``calculateCost`` action. Describes the cost of running
    a single model replica on the specified ``vmSize``. To estimate the cost of running multiple
    replicas, scale ``totalHourlyPrice`` by the desired replica count, bounded by
    ``maxAvailableReplicas``.

    :ivar vm_size: Azure VM SKU, e.g. "Standard_ND96isr_H100_v5". Matches the value accepted by
     ``ModelDeploymentProperties.vmSize``. Required.
    :vartype vm_size: str
    :ivar quantization: Resolved quantization on this SKU.
    :vartype quantization: str
    :ivar vms_per_replica: Number of VMs required to host one replica on this SKU. Required.
    :vartype vms_per_replica: int
    :ivar max_available_replicas: Maximum number of replicas the caller's subscription can deploy
     on this SKU today, computed from the available GPU quota in the target region. Required.
    :vartype max_available_replicas: int
    :ivar serving_performance_estimation: Estimated relative inference performance of a single
     model replica on this SKU. Omitted when an estimate is unavailable.
    :vartype serving_performance_estimation:
     ~azure.mgmt.containerserviceaimanager.models.ServingPerformanceEstimation
    :ivar vm_hourly_price: On-demand hourly price for a single VM of this SKU, in ``currency``.
     Required.
    :vartype vm_hourly_price: float
    :ivar total_hourly_price: Projected hourly cost for one replica (``vmsPerReplica`` VMs), in
     ``currency``.
    :vartype total_hourly_price: float
    :ivar price_as_of: UTC timestamp of the price snapshot used for this plan.
    :vartype price_as_of: ~datetime.datetime
    :ivar feasible: Whether the caller can actually deploy this plan today (region availability,
     GPU quota, model fit, etc.). This field gates the mutually exclusive properties on this model:

     * When `feasible` is `true`: `totalHourlyPrice` is set and `infeasibilityReason` is
       omitted.
     * When `feasible` is `false`: `infeasibilityReason` is set and `totalHourlyPrice` is
       omitted. Required.
    :vartype feasible: bool
    :ivar infeasibility_reason: Reason explaining why the plan is not deployable. This is a
     per-plan annotation, not an ARM error envelope.
    :vartype infeasibility_reason: ~azure.mgmt.containerserviceaimanager.models.InfeasibilityReason
    """

    vm_size: str = rest_field(name="vmSize", visibility=["read"])
    """Azure VM SKU, e.g. \"Standard_ND96isr_H100_v5\". Matches the value accepted by
     ``ModelDeploymentProperties.vmSize``. Required."""
    quantization: Optional[str] = rest_field(visibility=["read"])
    """Resolved quantization on this SKU."""
    vms_per_replica: int = rest_field(name="vmsPerReplica", visibility=["read"])
    """Number of VMs required to host one replica on this SKU. Required."""
    max_available_replicas: int = rest_field(name="maxAvailableReplicas", visibility=["read"])
    """Maximum number of replicas the caller's subscription can deploy on this SKU today, computed
     from the available GPU quota in the target region. Required."""
    serving_performance_estimation: Optional["_models.ServingPerformanceEstimation"] = rest_field(
        name="servingPerformanceEstimation", visibility=["read"]
    )
    """Estimated relative inference performance of a single model replica on this SKU. Omitted when an
     estimate is unavailable."""
    vm_hourly_price: float = rest_field(name="vmHourlyPrice", visibility=["read"])
    """On-demand hourly price for a single VM of this SKU, in ``currency``. Required."""
    total_hourly_price: Optional[float] = rest_field(name="totalHourlyPrice", visibility=["read"])
    """Projected hourly cost for one replica (``vmsPerReplica`` VMs), in ``currency``."""
    price_as_of: Optional[datetime.datetime] = rest_field(name="priceAsOf", visibility=["read"], format="rfc3339")
    """UTC timestamp of the price snapshot used for this plan."""
    feasible: bool = rest_field(visibility=["read"])
    """Whether the caller can actually deploy this plan today (region availability, GPU quota, model
      fit, etc.). This field gates the mutually exclusive properties on this model:
 
      * When `feasible` is `true`: `totalHourlyPrice` is set and `infeasibilityReason` is
        omitted.
      * When `feasible` is `false`: `infeasibilityReason` is set and `totalHourlyPrice` is
        omitted. Required."""
    infeasibility_reason: Optional["_models.InfeasibilityReason"] = rest_field(
        name="infeasibilityReason", visibility=["read"]
    )
    """Reason explaining why the plan is not deployable. This is a per-plan annotation, not an ARM
     error envelope."""


class CalculateCostRequest(_Model):
    """Request body for the AI model ``calculateCost`` action."""


class CalculateCostResponse(_Model):
    """Response body for the AI model ``calculateCost`` action.

    :ivar currency: ISO 4217 currency code, e.g. "USD". Required.
    :vartype currency: str
    :ivar plans: Ranked list of GPU SKU pricing plans. Feasible plans first, ordered by
     ``totalHourlyPrice`` ascending; infeasible plans last. Required.
    :vartype plans: list[~azure.mgmt.containerserviceaimanager.models.CalculateCostPlan]
    """

    currency: str = rest_field(visibility=["read"])
    """ISO 4217 currency code, e.g. \"USD\". Required."""
    plans: list["_models.CalculateCostPlan"] = rest_field(visibility=["read"])
    """Ranked list of GPU SKU pricing plans. Feasible plans first, ordered by ``totalHourlyPrice``
     ascending; infeasible plans last. Required."""


class CredentialResult(_Model):
    """The credential result response.

    :ivar name: The name of the credential.
    :vartype name: str
    :ivar value: Base64-encoded Kubernetes configuration file.
    :vartype value: bytes
    """

    name: Optional[str] = rest_field(visibility=["read"])
    """The name of the credential."""
    value: Optional[bytes] = rest_field(visibility=["read"], format="base64")
    """Base64-encoded Kubernetes configuration file."""


class CredentialResults(_Model):
    """The list credential result response.

    :ivar kubeconfigs: Array of credential results.
    :vartype kubeconfigs: list[~azure.mgmt.containerserviceaimanager.models.CredentialResult]
    """

    kubeconfigs: Optional[list["_models.CredentialResult"]] = rest_field(visibility=["read"])
    """Array of credential results."""


class CredentialValue(_Model):
    """A credential value. Exactly one variant must be set.

    In the current API version, only the ``inline`` variant is supported. Future
    API versions are expected to add additional credential kinds (for example,
    managed identity and Key Vault secret references) as sibling variants on
    this model.

    :ivar inline: An inline credential containing a secret value supplied in the request payload.
    :vartype inline: ~azure.mgmt.containerserviceaimanager.models.InlineCredential
    """

    inline: Optional["_models.InlineCredential"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """An inline credential containing a secret value supplied in the request payload."""

    @overload
    def __init__(
        self,
        *,
        inline: Optional["_models.InlineCredential"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ErrorAdditionalInfo(_Model):
    """The resource management error additional info.

    :ivar type: The additional info type.
    :vartype type: str
    :ivar info: The additional info.
    :vartype info: any
    """

    type: Optional[str] = rest_field(visibility=["read"])
    """The additional info type."""
    info: Optional[Any] = rest_field(visibility=["read"])
    """The additional info."""


class ErrorDetail(_Model):
    """The error detail.

    :ivar code: The error code.
    :vartype code: str
    :ivar message: The error message.
    :vartype message: str
    :ivar target: The error target.
    :vartype target: str
    :ivar details: The error details.
    :vartype details: list[~azure.mgmt.containerserviceaimanager.models.ErrorDetail]
    :ivar additional_info: The error additional info.
    :vartype additional_info:
     list[~azure.mgmt.containerserviceaimanager.models.ErrorAdditionalInfo]
    """

    code: Optional[str] = rest_field(visibility=["read"])
    """The error code."""
    message: Optional[str] = rest_field(visibility=["read"])
    """The error message."""
    target: Optional[str] = rest_field(visibility=["read"])
    """The error target."""
    details: Optional[list["_models.ErrorDetail"]] = rest_field(visibility=["read"])
    """The error details."""
    additional_info: Optional[list["_models.ErrorAdditionalInfo"]] = rest_field(
        name="additionalInfo", visibility=["read"]
    )
    """The error additional info."""


class ErrorResponse(_Model):
    """Error response.

    :ivar error: The error object.
    :vartype error: ~azure.mgmt.containerserviceaimanager.models.ErrorDetail
    """

    error: Optional["_models.ErrorDetail"] = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """The error object."""

    @overload
    def __init__(
        self,
        *,
        error: Optional["_models.ErrorDetail"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class InfeasibilityReason(_Model):
    """Reason explaining why a ``CalculateCostPlan`` is not deployable. This is a per-plan annotation
    surfaced inside a successful ``calculateCost`` response, not an ARM error envelope.

    :ivar code: Machine-readable reason code. Required. Known values are: "InsufficientQuota",
     "RegionUnavailable", and "InefficientDeployment".
    :vartype code: str or ~azure.mgmt.containerserviceaimanager.models.InfeasibleCode
    :ivar message: Human-readable message accompanying ``code``. Required.
    :vartype message: str
    """

    code: Union[str, "_models.InfeasibleCode"] = rest_field(visibility=["read"])
    """Machine-readable reason code. Required. Known values are: \"InsufficientQuota\",
     \"RegionUnavailable\", and \"InefficientDeployment\"."""
    message: str = rest_field(visibility=["read"])
    """Human-readable message accompanying ``code``. Required."""


class InlineCredential(_Model):
    """A credential provided inline.

    :ivar value: The access token, password, or other secret value. Required.
    :vartype value: str
    """

    value: str = rest_field(visibility=["create", "update"])
    """The access token, password, or other secret value. Required."""

    @overload
    def __init__(
        self,
        *,
        value: str,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ManagedServiceIdentity(_Model):
    """Managed service identity (system assigned and/or user assigned identities).

    :ivar principal_id: The service principal ID of the system assigned identity. This property
     will only be provided for a system assigned identity.
    :vartype principal_id: str
    :ivar tenant_id: The tenant ID of the system assigned identity. This property will only be
     provided for a system assigned identity.
    :vartype tenant_id: str
    :ivar type: The type of managed identity assigned to this resource. Required. Known values are:
     "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".
    :vartype type: str or ~azure.mgmt.containerserviceaimanager.models.ManagedServiceIdentityType
    :ivar user_assigned_identities: The identities assigned to this resource by the user.
    :vartype user_assigned_identities: dict[str,
     ~azure.mgmt.containerserviceaimanager.models.UserAssignedIdentity]
    """

    principal_id: Optional[str] = rest_field(name="principalId", visibility=["read"])
    """The service principal ID of the system assigned identity. This property will only be provided
     for a system assigned identity."""
    tenant_id: Optional[str] = rest_field(name="tenantId", visibility=["read"])
    """The tenant ID of the system assigned identity. This property will only be provided for a system
     assigned identity."""
    type: Union[str, "_models.ManagedServiceIdentityType"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The type of managed identity assigned to this resource. Required. Known values are: \"None\",
     \"SystemAssigned\", \"UserAssigned\", and \"SystemAssigned,UserAssigned\"."""
    user_assigned_identities: Optional[dict[str, "_models.UserAssignedIdentity"]] = rest_field(
        name="userAssignedIdentities", visibility=["read", "create", "update", "delete", "query"]
    )
    """The identities assigned to this resource by the user."""

    @overload
    def __init__(
        self,
        *,
        type: Union[str, "_models.ManagedServiceIdentityType"],
        user_assigned_identities: Optional[dict[str, "_models.UserAssignedIdentity"]] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ManualScalingProfile(_Model):
    """Manual scaling configuration: fixed replica count.

    :ivar replicas: Fixed number of replicas. May be ``0`` to stop serving traffic while keeping
     the deployment configuration (see ``ScalingProfile``). Required.
    :vartype replicas: int
    """

    replicas: int = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """Fixed number of replicas. May be ``0`` to stop serving traffic while keeping the deployment
     configuration (see ``ScalingProfile``). Required."""

    @overload
    def __init__(
        self,
        *,
        replicas: int,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ModelDeployment(ProxyResource):
    """A running deployment of a model in an AI Manager namespace.

    PUT (create or update) on this resource is a full replace: the request body
    represents the complete desired state, and any optional property omitted
    from the body is reset to its default value (or cleared, if it has no
    default). Callers must always send the full desired state on every PUT.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: ~azure.mgmt.containerserviceaimanager.models.ModelDeploymentProperties
    :ivar e_tag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype e_tag: str
    """

    properties: Optional["_models.ModelDeploymentProperties"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The resource-specific properties for this resource."""
    e_tag: Optional[str] = rest_field(name="eTag", visibility=["read"])
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""

    @overload
    def __init__(
        self,
        *,
        properties: Optional["_models.ModelDeploymentProperties"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ModelDeploymentOverrides(_Model):
    """User overrides for a model deployment.

    :ivar values_property: Experimental free-form override key/value pairs. Subject to change
     without notice; not part of the stable contract. Recognized keys are documented per release and
     may be added, renamed, or removed at any time.
    :vartype values_property: dict[str, str]
    """

    values_property: Optional[dict[str, str]] = rest_field(
        name="values", visibility=["read", "create", "update", "delete", "query"], original_tsp_name="values"
    )
    """Experimental free-form override key/value pairs. Subject to change without notice; not part of
     the stable contract. Recognized keys are documented per release and may be added, renamed, or
     removed at any time."""

    @overload
    def __init__(
        self,
        *,
        values_property: Optional[dict[str, str]] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ModelDeploymentProperties(_Model):
    """Model deployment properties.

    :ivar provisioning_state: The status of the last reconciliation. Known values are: "Succeeded",
     "Failed", "Canceled", "Creating", "Updating", and "Deleting".
    :vartype provisioning_state: str or
     ~azure.mgmt.containerserviceaimanager.models.ModelDeploymentProvisioningState
    :ivar model_resource_id: Full ARM resource id of the model to deploy. Phase 1 accepts an
     ``AIModel`` resource id only. Immutable after creation. Required.
    :vartype model_resource_id: str
    :ivar model_source_resource_id: Full ARM resource id of a ``ModelSource`` to use when pulling
     artifacts for this deployment. Immutable after creation.
    :vartype model_source_resource_id: str
    :ivar performance_mode: Runtime performance mode. Known values are: "Balanced", "Latency", and
     "Throughput".
    :vartype performance_mode: str or
     ~azure.mgmt.containerserviceaimanager.models.ModelDeploymentPerformanceMode
    :ivar vm_size: Azure VM SKU used to host the deployment, e.g. "Standard_NC96ads_A100_v4".
     Immutable after creation. Required.
    :vartype vm_size: str
    :ivar scale: Scaling configuration for the deployment. Provide either ``manual`` (fixed replica
     count) or ``autoscale`` (autoscaling between min/max replicas), but not both.
    :vartype scale: ~azure.mgmt.containerserviceaimanager.models.ScalingProfile
    :ivar overrides: User overrides layered on top of profile resolution.
    :vartype overrides: ~azure.mgmt.containerserviceaimanager.models.ModelDeploymentOverrides
    :ivar status: Runtime status, populated once reconciliation begins.
    :vartype status: ~azure.mgmt.containerserviceaimanager.models.ModelDeploymentStatus
    """

    provisioning_state: Optional[Union[str, "_models.ModelDeploymentProvisioningState"]] = rest_field(
        name="provisioningState", visibility=["read"]
    )
    """The status of the last reconciliation. Known values are: \"Succeeded\", \"Failed\",
     \"Canceled\", \"Creating\", \"Updating\", and \"Deleting\"."""
    model_resource_id: str = rest_field(name="modelResourceId", visibility=["read", "create"])
    """Full ARM resource id of the model to deploy. Phase 1 accepts an ``AIModel`` resource id only.
     Immutable after creation. Required."""
    model_source_resource_id: Optional[str] = rest_field(name="modelSourceResourceId", visibility=["read", "create"])
    """Full ARM resource id of a ``ModelSource`` to use when pulling artifacts for this deployment.
     Immutable after creation."""
    performance_mode: Optional[Union[str, "_models.ModelDeploymentPerformanceMode"]] = rest_field(
        name="performanceMode", visibility=["read", "create", "update", "delete", "query"]
    )
    """Runtime performance mode. Known values are: \"Balanced\", \"Latency\", and \"Throughput\"."""
    vm_size: str = rest_field(name="vmSize", visibility=["read", "create"])
    """Azure VM SKU used to host the deployment, e.g. \"Standard_NC96ads_A100_v4\". Immutable after
     creation. Required."""
    scale: Optional["_models.ScalingProfile"] = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """Scaling configuration for the deployment. Provide either ``manual`` (fixed replica count) or
     ``autoscale`` (autoscaling between min/max replicas), but not both."""
    overrides: Optional["_models.ModelDeploymentOverrides"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """User overrides layered on top of profile resolution."""
    status: Optional["_models.ModelDeploymentStatus"] = rest_field(visibility=["read"])
    """Runtime status, populated once reconciliation begins."""

    @overload
    def __init__(
        self,
        *,
        model_resource_id: str,
        vm_size: str,
        model_source_resource_id: Optional[str] = None,
        performance_mode: Optional[Union[str, "_models.ModelDeploymentPerformanceMode"]] = None,
        scale: Optional["_models.ScalingProfile"] = None,
        overrides: Optional["_models.ModelDeploymentOverrides"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ModelDeploymentStatus(_Model):
    """The runtime status of a model deployment. All fields are read-only and populated once
    reconciliation has started.

    :ivar endpoint: The inference endpoint URL exposed by the deployment, once ready.
    :vartype endpoint: str
    :ivar engine: The inference engine used to serve the model, e.g. "vllm".
    :vartype engine: str
    :ivar engine_version: The version of the inference engine, e.g. "0.17".
    :vartype engine_version: str
    :ivar max_model_len: The maximum model context length, in tokens, configured for this
     deployment.
    :vartype max_model_len: int
    :ivar quantization: The quantization level applied to the model weights, e.g. "fp16",
     "awq-int4".
    :vartype quantization: str
    :ivar desired_replicas: The desired replica count reported by the controller. Equals
     ``properties.scale.manual.replicas`` when manual scaling is used; current target replica count
     derived from autoscaler otherwise.
    :vartype desired_replicas: int
    :ivar current_replicas: The current number of ready replicas serving traffic.
    :vartype current_replicas: int
    :ivar peak_tokens_per_minute: The peak tokens per minute measured by live stress test.
    :vartype peak_tokens_per_minute: int
    :ivar estimated_provision_time_seconds: Estimated total time, in seconds, for the deployment to
     become ready end-to-end (GPU vm provisioning, image/weight pull, engine warm-up).
    :vartype estimated_provision_time_seconds: int
    """

    endpoint: Optional[str] = rest_field(visibility=["read"])
    """The inference endpoint URL exposed by the deployment, once ready."""
    engine: Optional[str] = rest_field(visibility=["read"])
    """The inference engine used to serve the model, e.g. \"vllm\"."""
    engine_version: Optional[str] = rest_field(name="engineVersion", visibility=["read"])
    """The version of the inference engine, e.g. \"0.17\"."""
    max_model_len: Optional[int] = rest_field(name="maxModelLen", visibility=["read"])
    """The maximum model context length, in tokens, configured for this deployment."""
    quantization: Optional[str] = rest_field(visibility=["read"])
    """The quantization level applied to the model weights, e.g. \"fp16\", \"awq-int4\"."""
    desired_replicas: Optional[int] = rest_field(name="desiredReplicas", visibility=["read"])
    """The desired replica count reported by the controller. Equals
     ``properties.scale.manual.replicas`` when manual scaling is used; current target replica count
     derived from autoscaler otherwise."""
    current_replicas: Optional[int] = rest_field(name="currentReplicas", visibility=["read"])
    """The current number of ready replicas serving traffic."""
    peak_tokens_per_minute: Optional[int] = rest_field(name="peakTokensPerMinute", visibility=["read"])
    """The peak tokens per minute measured by live stress test."""
    estimated_provision_time_seconds: Optional[int] = rest_field(
        name="estimatedProvisionTimeSeconds", visibility=["read"]
    )
    """Estimated total time, in seconds, for the deployment to become ready end-to-end (GPU vm
     provisioning, image/weight pull, engine warm-up)."""


class ModelSource(ProxyResource):
    """A model source registered with an AI Manager. Describes an external model registry (e.g.
    Hugging Face) and the credentials the platform uses to pull artifacts from it.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar system_data: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype system_data: ~azure.mgmt.containerserviceaimanager.models.SystemData
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: ~azure.mgmt.containerserviceaimanager.models.ModelSourceProperties
    :ivar e_tag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype e_tag: str
    """

    properties: Optional["_models.ModelSourceProperties"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """The resource-specific properties for this resource."""
    e_tag: Optional[str] = rest_field(name="eTag", visibility=["read"])
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""

    @overload
    def __init__(
        self,
        *,
        properties: Optional["_models.ModelSourceProperties"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ModelSourceProperties(_Model):
    """Model source properties.

    :ivar provisioning_state: The status of the last operation. Known values are: "Succeeded",
     "Failed", and "Canceled".
    :vartype provisioning_state: str or
     ~azure.mgmt.containerserviceaimanager.models.ResourceProvisioningState
    :ivar source_type: Model source type. Constrains the legal authentication kinds. Immutable
     after creation. Required. "HuggingFace"
    :vartype source_type: str or ~azure.mgmt.containerserviceaimanager.models.ModelSourceType
    :ivar description: An optional, free-form description of the source.
    :vartype description: str
    :ivar credential: Credential the platform uses to authenticate to the source. Optional for
     public sources (e.g. ungated Hugging Face models).
    :vartype credential: ~azure.mgmt.containerserviceaimanager.models.CredentialValue
    """

    provisioning_state: Optional[Union[str, "_models.ResourceProvisioningState"]] = rest_field(
        name="provisioningState", visibility=["read"]
    )
    """The status of the last operation. Known values are: \"Succeeded\", \"Failed\", and
     \"Canceled\"."""
    source_type: Union[str, "_models.ModelSourceType"] = rest_field(name="sourceType", visibility=["read", "create"])
    """Model source type. Constrains the legal authentication kinds. Immutable after creation.
     Required. \"HuggingFace\""""
    description: Optional[str] = rest_field(visibility=["read", "create", "update", "delete", "query"])
    """An optional, free-form description of the source."""
    credential: Optional["_models.CredentialValue"] = rest_field(visibility=["create", "update"])
    """Credential the platform uses to authenticate to the source. Optional for public sources (e.g.
     ungated Hugging Face models)."""

    @overload
    def __init__(
        self,
        *,
        source_type: Union[str, "_models.ModelSourceType"],
        description: Optional[str] = None,
        credential: Optional["_models.CredentialValue"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ModelSpec(_Model):
    """The specification of a model. All fields are read-only.

    :ivar license: The license of the model, when known. SPDX license identifier, e.g. ``mit``,
     ``apache-2.0``.
    :vartype license: str
    :ivar is_restricted: Whether access to the model is restricted and requires credential.
     Required.
    :vartype is_restricted: bool
    :ivar max_context_length: The maximum context length supported by the model, in tokens.
     Required.
    :vartype max_context_length: int
    """

    license: Optional[str] = rest_field(visibility=["read"])
    """The license of the model, when known. SPDX license identifier, e.g. ``mit``, ``apache-2.0``."""
    is_restricted: bool = rest_field(name="isRestricted", visibility=["read"])
    """Whether access to the model is restricted and requires credential. Required."""
    max_context_length: int = rest_field(name="maxContextLength", visibility=["read"])
    """The maximum context length supported by the model, in tokens. Required."""


class NamespaceAccessInfo(_Model):
    """Access information for an AI Manager namespace, including the OpenAI-compatible gateway
    endpoint and the API keys used to authenticate against it.

    :ivar endpoint: OpenAI-compatible inference gateway base URL (for example,
     ``https://team-alpha.<cluster>.<region>.aksapp.io/v1``). Required.
    :vartype endpoint: str
    :ivar primary_key: Primary API key. Send as ``Authorization: Bearer <key>`` or ``api-key:
     <key>``. Treat as secret; do not log or persist in plaintext. Required.
    :vartype primary_key: str
    :ivar secondary_key: Secondary API key, accepted by the gateway in the same headers as
     ``primaryKey``. Generated independently when the namespace is created, then overwritten by the
     previous ``primaryKey`` on each ``rotateKeys`` call so clients can roll over without downtime.
     Treat as secret; do not log or persist in plaintext. Required.
    :vartype secondary_key: str
    :ivar last_rotated_at: UTC time the keys were last rotated by ``rotateKeys``. Absent until the
     first rotation. Clients can use this to detect rotation and refresh cached credentials.
    :vartype last_rotated_at: ~datetime.datetime
    """

    endpoint: str = rest_field(visibility=["read"])
    """OpenAI-compatible inference gateway base URL (for example,
     ``https://team-alpha.<cluster>.<region>.aksapp.io/v1``). Required."""
    primary_key: str = rest_field(name="primaryKey", visibility=["read"])
    """Primary API key. Send as ``Authorization: Bearer <key>`` or ``api-key: <key>``. Treat as
     secret; do not log or persist in plaintext. Required."""
    secondary_key: str = rest_field(name="secondaryKey", visibility=["read"])
    """Secondary API key, accepted by the gateway in the same headers as ``primaryKey``. Generated
     independently when the namespace is created, then overwritten by the previous ``primaryKey`` on
     each ``rotateKeys`` call so clients can roll over without downtime. Treat as secret; do not log
     or persist in plaintext. Required."""
    last_rotated_at: Optional[datetime.datetime] = rest_field(
        name="lastRotatedAt", visibility=["read"], format="rfc3339"
    )
    """UTC time the keys were last rotated by ``rotateKeys``. Absent until the first rotation. Clients
     can use this to detect rotation and refresh cached credentials."""


class Operation(_Model):
    """REST API Operation.

    :ivar name: The name of the operation, as per Resource-Based Access Control (RBAC). Examples:
     "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".
    :vartype name: str
    :ivar is_data_action: Whether the operation applies to data-plane. This is "true" for
     data-plane operations and "false" for Azure Resource Manager/control-plane operations.
    :vartype is_data_action: bool
    :ivar display: Localized display information for this particular operation.
    :vartype display: ~azure.mgmt.containerserviceaimanager.models.OperationDisplay
    :ivar origin: The intended executor of the operation; as in Resource Based Access Control
     (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system",
     and "user,system".
    :vartype origin: str or ~azure.mgmt.containerserviceaimanager.models.Origin
    :ivar action_type: Extensible enum. Indicates the action type. "Internal" refers to actions
     that are for internal only APIs. "Internal"
    :vartype action_type: str or ~azure.mgmt.containerserviceaimanager.models.ActionType
    """

    name: Optional[str] = rest_field(visibility=["read"])
    """The name of the operation, as per Resource-Based Access Control (RBAC). Examples:
     \"Microsoft.Compute/virtualMachines/write\",
     \"Microsoft.Compute/virtualMachines/capture/action\"."""
    is_data_action: Optional[bool] = rest_field(name="isDataAction", visibility=["read"])
    """Whether the operation applies to data-plane. This is \"true\" for data-plane operations and
     \"false\" for Azure Resource Manager/control-plane operations."""
    display: Optional["_models.OperationDisplay"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """Localized display information for this particular operation."""
    origin: Optional[Union[str, "_models.Origin"]] = rest_field(visibility=["read"])
    """The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit
     logs UX. Default value is \"user,system\". Known values are: \"user\", \"system\", and
     \"user,system\"."""
    action_type: Optional[Union[str, "_models.ActionType"]] = rest_field(name="actionType", visibility=["read"])
    """Extensible enum. Indicates the action type. \"Internal\" refers to actions that are for
     internal only APIs. \"Internal\""""

    @overload
    def __init__(
        self,
        *,
        display: Optional["_models.OperationDisplay"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class OperationDisplay(_Model):
    """Localized display information for an operation.

    :ivar provider: The localized friendly form of the resource provider name, e.g. "Microsoft
     Monitoring Insights" or "Microsoft Compute".
    :vartype provider: str
    :ivar resource: The localized friendly name of the resource type related to this operation.
     E.g. "Virtual Machines" or "Job Schedule Collections".
    :vartype resource: str
    :ivar operation: The concise, localized friendly name for the operation; suitable for
     dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
    :vartype operation: str
    :ivar description: The short, localized friendly description of the operation; suitable for
     tool tips and detailed views.
    :vartype description: str
    """

    provider: Optional[str] = rest_field(visibility=["read"])
    """The localized friendly form of the resource provider name, e.g. \"Microsoft Monitoring
     Insights\" or \"Microsoft Compute\"."""
    resource: Optional[str] = rest_field(visibility=["read"])
    """The localized friendly name of the resource type related to this operation. E.g. \"Virtual
     Machines\" or \"Job Schedule Collections\"."""
    operation: Optional[str] = rest_field(visibility=["read"])
    """The concise, localized friendly name for the operation; suitable for dropdowns. E.g. \"Create
     or Update Virtual Machine\", \"Restart Virtual Machine\"."""
    description: Optional[str] = rest_field(visibility=["read"])
    """The short, localized friendly description of the operation; suitable for tool tips and detailed
     views."""


class ScalingProfile(_Model):
    """Scaling configuration for a model deployment. Exactly one of ``manual`` or
    ``autoscale`` must be set.

    This mutual-exclusion constraint is enforced by the service at request
    validation time, not by the schema. A PUT request that sets both ``manual``
    and ``autoscale``, or sets neither, is rejected with HTTP 400 (Bad Request)
    and an ``InvalidScalingProfile`` error code;

    Scale-to-zero semantics differ between the two modes:

    * `manual` permits `replicas: 0`. This is an explicit operator action to
    stop serving traffic while keeping the `ModelDeployment` resource (and
    its configuration) in place. While at zero replicas the endpoint
    returns errors for inference requests, and the deployment releases its
    GPU capacity.
    * `autoscale` does not permit `minReplicas: 0`. Autoscaling decisions are
    driven by serving-server runtime metrics (request rate, queue depth,
    GPU utilization); at zero replicas there is no signal for the
    autoscaler to scale back up from. Combined with GPU cold-start time
    (on the order of minutes) and constrained regional GPU capacity, a
    scale-from-zero event would produce unacceptable first-request latency
    and a high risk of capacity unavailability. Callers that want
    autoscaling with an idle state should delete the `ModelDeployment`
    instead.

    :ivar manual: Manual scaling configuration with a fixed replica count. Mutually exclusive with
     ``autoscale``.
    :vartype manual: ~azure.mgmt.containerserviceaimanager.models.ManualScalingProfile
    :ivar autoscale: Autoscaling configuration. Mutually exclusive with ``manual``.
    :vartype autoscale: ~azure.mgmt.containerserviceaimanager.models.AutoscaleProfile
    """

    manual: Optional["_models.ManualScalingProfile"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """Manual scaling configuration with a fixed replica count. Mutually exclusive with ``autoscale``."""
    autoscale: Optional["_models.AutoscaleProfile"] = rest_field(
        visibility=["read", "create", "update", "delete", "query"]
    )
    """Autoscaling configuration. Mutually exclusive with ``manual``."""

    @overload
    def __init__(
        self,
        *,
        manual: Optional["_models.ManualScalingProfile"] = None,
        autoscale: Optional["_models.AutoscaleProfile"] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class ServingPerformanceEstimation(_Model):
    """Estimated relative inference performance of a single model replica on a given GPU SKU. Each
    metric is a scaling coefficient in the range ``[0, 1]`` relative to the best-performing SKU for
    this model, which scores ``1``.

    :ivar relative_latency_score: Relative inference latency score in ``[0, 1]``. Higher is better
     (``1`` matches the best-performing SKU's latency for this model). Note: this is a normalized
     score, not a raw latency ratio -- a larger value indicates lower latency. Required.
    :vartype relative_latency_score: float
    :ivar relative_throughput_score: Relative inference throughput score in ``[0, 1]``. Higher is
     better (``1`` matches the best-performing SKU's throughput for this model). Required.
    :vartype relative_throughput_score: float
    """

    relative_latency_score: float = rest_field(name="relativeLatencyScore", visibility=["read"])
    """Relative inference latency score in ``[0, 1]``. Higher is better (``1`` matches the
     best-performing SKU's latency for this model). Note: this is a normalized score, not a raw
     latency ratio -- a larger value indicates lower latency. Required."""
    relative_throughput_score: float = rest_field(name="relativeThroughputScore", visibility=["read"])
    """Relative inference throughput score in ``[0, 1]``. Higher is better (``1`` matches the
     best-performing SKU's throughput for this model). Required."""


class SystemData(_Model):
    """Metadata pertaining to creation and last modification of the resource.

    :ivar created_by: The identity that created the resource.
    :vartype created_by: str
    :ivar created_by_type: The type of identity that created the resource. Known values are:
     "User", "Application", "ManagedIdentity", and "Key".
    :vartype created_by_type: str or ~azure.mgmt.containerserviceaimanager.models.CreatedByType
    :ivar created_at: The timestamp of resource creation (UTC).
    :vartype created_at: ~datetime.datetime
    :ivar last_modified_by: The identity that last modified the resource.
    :vartype last_modified_by: str
    :ivar last_modified_by_type: The type of identity that last modified the resource. Known values
     are: "User", "Application", "ManagedIdentity", and "Key".
    :vartype last_modified_by_type: str or
     ~azure.mgmt.containerserviceaimanager.models.CreatedByType
    :ivar last_modified_at: The timestamp of resource last modification (UTC).
    :vartype last_modified_at: ~datetime.datetime
    """

    created_by: Optional[str] = rest_field(name="createdBy", visibility=["read", "create", "update", "delete", "query"])
    """The identity that created the resource."""
    created_by_type: Optional[Union[str, "_models.CreatedByType"]] = rest_field(
        name="createdByType", visibility=["read", "create", "update", "delete", "query"]
    )
    """The type of identity that created the resource. Known values are: \"User\", \"Application\",
     \"ManagedIdentity\", and \"Key\"."""
    created_at: Optional[datetime.datetime] = rest_field(
        name="createdAt", visibility=["read", "create", "update", "delete", "query"], format="rfc3339"
    )
    """The timestamp of resource creation (UTC)."""
    last_modified_by: Optional[str] = rest_field(
        name="lastModifiedBy", visibility=["read", "create", "update", "delete", "query"]
    )
    """The identity that last modified the resource."""
    last_modified_by_type: Optional[Union[str, "_models.CreatedByType"]] = rest_field(
        name="lastModifiedByType", visibility=["read", "create", "update", "delete", "query"]
    )
    """The type of identity that last modified the resource. Known values are: \"User\",
     \"Application\", \"ManagedIdentity\", and \"Key\"."""
    last_modified_at: Optional[datetime.datetime] = rest_field(
        name="lastModifiedAt", visibility=["read", "create", "update", "delete", "query"], format="rfc3339"
    )
    """The timestamp of resource last modification (UTC)."""

    @overload
    def __init__(
        self,
        *,
        created_by: Optional[str] = None,
        created_by_type: Optional[Union[str, "_models.CreatedByType"]] = None,
        created_at: Optional[datetime.datetime] = None,
        last_modified_by: Optional[str] = None,
        last_modified_by_type: Optional[Union[str, "_models.CreatedByType"]] = None,
        last_modified_at: Optional[datetime.datetime] = None,
    ) -> None: ...

    @overload
    def __init__(self, mapping: Mapping[str, Any]) -> None:
        """
        :param mapping: raw JSON to initialize the model.
        :type mapping: Mapping[str, Any]
        """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)


class UserAssignedIdentity(_Model):
    """User assigned identity properties.

    :ivar principal_id: The principal ID of the assigned identity.
    :vartype principal_id: str
    :ivar client_id: The client ID of the assigned identity.
    :vartype client_id: str
    """

    principal_id: Optional[str] = rest_field(name="principalId", visibility=["read"])
    """The principal ID of the assigned identity."""
    client_id: Optional[str] = rest_field(name="clientId", visibility=["read"])
    """The client ID of the assigned identity."""
