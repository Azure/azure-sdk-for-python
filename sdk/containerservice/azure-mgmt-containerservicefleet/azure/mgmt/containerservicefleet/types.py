# pylint: disable=line-too-long,useless-suppression,too-many-lines
# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
# Code generated by Microsoft (R) Python Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is regenerated.
# --------------------------------------------------------------------------

from typing import Any, TYPE_CHECKING, Union
from typing_extensions import Required, TypedDict

if TYPE_CHECKING:
    from .models import (
        AdoptionPolicy,
        AutoUpgradeLastTriggerStatus,
        AutoUpgradeNodeImageSelectionType,
        AutoUpgradeProfileProvisioningState,
        CreatedByType,
        DeletePolicy,
        FleetManagedNamespaceProvisioningState,
        FleetMemberProvisioningState,
        FleetProvisioningState,
        FleetUpdateStrategyProvisioningState,
        GateState,
        GateType,
        LabelSelectorOperator,
        ManagedClusterUpgradeType,
        ManagedServiceIdentityType,
        NodeImageSelectionType,
        PlacementType,
        PolicyRule,
        PropagationType,
        PropertySelectorOperator,
        TaintEffect,
        TargetType,
        TolerationOperator,
        UpdateRunProvisioningState,
        UpdateState,
        UpgradeChannel,
    )


class Affinity(TypedDict, total=False):
    """Affinity is a group of cluster affinity scheduling rules. More to be added.

    :ivar clusterAffinity: ClusterAffinity contains cluster affinity scheduling rules for the
     selected resources.
    :vartype clusterAffinity: "ClusterAffinity"
    """

    clusterAffinity: "ClusterAffinity"
    """ClusterAffinity contains cluster affinity scheduling rules for the selected resources."""


class AgentProfile(TypedDict, total=False):
    """Agent profile for the Fleet hub.

    :ivar subnetId: The ID of the subnet which the Fleet hub node will join on startup. If this is
     not specified, a vnet and subnet will be generated and used.
    :vartype subnetId: str
    :ivar vmSize: The virtual machine size of the Fleet hub.
    :vartype vmSize: str
    """

    subnetId: str
    """The ID of the subnet which the Fleet hub node will join on startup. If this is not specified, a
     vnet and subnet will be generated and used."""
    vmSize: str
    """The virtual machine size of the Fleet hub."""


class APIServerAccessProfile(TypedDict, total=False):
    """Access profile for the Fleet hub API server.

    :ivar enablePrivateCluster: Whether to create the Fleet hub as a private cluster or not.
    :vartype enablePrivateCluster: bool
    :ivar enableVnetIntegration: Whether to enable apiserver vnet integration for the Fleet hub or
     not.
    :vartype enableVnetIntegration: bool
    :ivar subnetId: The subnet to be used when apiserver vnet integration is enabled. It is
     required when creating a new Fleet with BYO vnet.
    :vartype subnetId: str
    """

    enablePrivateCluster: bool
    """Whether to create the Fleet hub as a private cluster or not."""
    enableVnetIntegration: bool
    """Whether to enable apiserver vnet integration for the Fleet hub or not."""
    subnetId: str
    """The subnet to be used when apiserver vnet integration is enabled. It is required when creating
     a new Fleet with BYO vnet."""


class AutoUpgradeNodeImageSelection(TypedDict, total=False):
    """The node image upgrade to be applied to the target clusters in auto upgrade.

    :ivar type: The node image upgrade type. Required. Known values are: "Latest" and "Consistent".
    :vartype type: Union[str, "AutoUpgradeNodeImageSelectionType"]
    """

    type: Required[Union[str, "AutoUpgradeNodeImageSelectionType"]]
    """The node image upgrade type. Required. Known values are: \"Latest\" and \"Consistent\"."""


class Resource(TypedDict, total=False):
    """Resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    """

    id: str
    """Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}."""
    name: str
    """The name of the resource."""
    type: str
    """The type of the resource. E.g. \"Microsoft.Compute/virtualMachines\" or
     \"Microsoft.Storage/storageAccounts\"."""
    systemData: "SystemData"
    """Azure Resource Manager metadata containing createdBy and modifiedBy information."""


class ProxyResource(Resource):
    """Proxy Resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    """


class AutoUpgradeProfile(ProxyResource):
    """The AutoUpgradeProfile resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: "AutoUpgradeProfileProperties"
    :ivar eTag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype eTag: str
    """

    properties: "AutoUpgradeProfileProperties"
    """The resource-specific properties for this resource."""
    eTag: str
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""


class AutoUpgradeProfileProperties(TypedDict, total=False):
    """The properties of the AutoUpgradeProfile.

    :ivar provisioningState: The provisioning state of the AutoUpgradeProfile resource. Known
     values are: "Succeeded", "Failed", and "Canceled".
    :vartype provisioningState: Union[str, "AutoUpgradeProfileProvisioningState"]
    :ivar updateStrategyId: The resource id of the UpdateStrategy resource to reference. If not
     specified, the auto upgrade will run on all clusters which are members of the fleet.
    :vartype updateStrategyId: str
    :ivar channel: Configures how auto-upgrade will be run. Required. Known values are: "Stable",
     "Rapid", "NodeImage", and "TargetKubernetesVersion".
    :vartype channel: Union[str, "UpgradeChannel"]
    :ivar nodeImageSelection: The node image upgrade to be applied to the target clusters in auto
     upgrade.
    :vartype nodeImageSelection: "AutoUpgradeNodeImageSelection"
    :ivar disabled: If set to False: the auto upgrade has effect - target managed clusters will be
     upgraded on schedule. If set to True: the auto upgrade has no effect - no upgrade will be run
     on the target managed clusters. This is a boolean and not an enum because enabled/disabled are
     all available states of the auto upgrade profile. By default, this is set to False.
    :vartype disabled: bool
    :ivar autoUpgradeProfileStatus: The status of the auto upgrade profile.
    :vartype autoUpgradeProfileStatus: "AutoUpgradeProfileStatus"
    :ivar targetKubernetesVersion:   This is the target Kubernetes version for auto-upgrade. The
     format must be ``{major version}.{minor version}``. For example, "1.30". By default, this is
     empty. If upgrade channel is set to TargetKubernetesVersion, this field must not be empty. If
     upgrade channel is Rapid, Stable or NodeImage, this field must be empty.
    :vartype targetKubernetesVersion: str
    :ivar longTermSupport:   If upgrade channel is not TargetKubernetesVersion, this field must be
     False. If set to True: Fleet auto upgrade will continue generate update runs for patches of
     minor versions earlier than N-2 (where N is the latest supported minor version) if those minor
     versions support Long-Term Support (LTS). By default, this is set to False. For more
     information on AKS LTS, please see
     `https://learn.microsoft.com/en-us/azure/aks/long-term-support
     <https://learn.microsoft.com/en-us/azure/aks/long-term-support>`_.
    :vartype longTermSupport: bool
    """

    provisioningState: Union[str, "AutoUpgradeProfileProvisioningState"]
    """The provisioning state of the AutoUpgradeProfile resource. Known values are: \"Succeeded\",
     \"Failed\", and \"Canceled\"."""
    updateStrategyId: str
    """The resource id of the UpdateStrategy resource to reference. If not specified, the auto upgrade
     will run on all clusters which are members of the fleet."""
    channel: Required[Union[str, "UpgradeChannel"]]
    """Configures how auto-upgrade will be run. Required. Known values are: \"Stable\", \"Rapid\",
     \"NodeImage\", and \"TargetKubernetesVersion\"."""
    nodeImageSelection: "AutoUpgradeNodeImageSelection"
    """The node image upgrade to be applied to the target clusters in auto upgrade."""
    disabled: bool
    """If set to False: the auto upgrade has effect - target managed clusters will be upgraded on
     schedule. If set to True: the auto upgrade has no effect - no upgrade will be run on the target
     managed clusters. This is a boolean and not an enum because enabled/disabled are all available
     states of the auto upgrade profile. By default, this is set to False."""
    autoUpgradeProfileStatus: "AutoUpgradeProfileStatus"
    """The status of the auto upgrade profile."""
    targetKubernetesVersion: str
    """This is the target Kubernetes version for auto-upgrade. The format must be ``{major
     version}.{minor version}``. For example, \"1.30\". By default, this is empty. If upgrade
     channel is set to TargetKubernetesVersion, this field must not be empty. If upgrade channel is
     Rapid, Stable or NodeImage, this field must be empty."""
    longTermSupport: bool
    """If upgrade channel is not TargetKubernetesVersion, this field must be False. If set to True:
     Fleet auto upgrade will continue generate update runs for patches of minor versions earlier
     than N-2 (where N is the latest supported minor version) if those minor versions support
     Long-Term Support (LTS). By default, this is set to False. For more information on AKS LTS,
     please see `https://learn.microsoft.com/en-us/azure/aks/long-term-support
     <https://learn.microsoft.com/en-us/azure/aks/long-term-support>`_."""


class AutoUpgradeProfileStatus(TypedDict, total=False):
    """AutoUpgradeProfileStatus is the status of an auto upgrade profile.

    :ivar lastTriggeredAt: The UTC time of the last attempt to automatically create and start an
     UpdateRun as triggered by the release of new versions.
    :vartype lastTriggeredAt: str
    :ivar lastTriggerStatus: The status of the last AutoUpgrade trigger. Known values are:
     "Succeeded" and "Failed".
    :vartype lastTriggerStatus: Union[str, "AutoUpgradeLastTriggerStatus"]
    :ivar lastTriggerError: The error details of the last trigger.
    :vartype lastTriggerError: "ErrorDetail"
    :ivar lastTriggerUpgradeVersions: The target Kubernetes version or node image versions of the
     last trigger.
    :vartype lastTriggerUpgradeVersions: list[str]
    """

    lastTriggeredAt: str
    """The UTC time of the last attempt to automatically create and start an UpdateRun as triggered by
     the release of new versions."""
    lastTriggerStatus: Union[str, "AutoUpgradeLastTriggerStatus"]
    """The status of the last AutoUpgrade trigger. Known values are: \"Succeeded\" and \"Failed\"."""
    lastTriggerError: "ErrorDetail"
    """The error details of the last trigger."""
    lastTriggerUpgradeVersions: list[str]
    """The target Kubernetes version or node image versions of the last trigger."""


class ClusterAffinity(TypedDict, total=False):
    """ClusterAffinity contains cluster affinity scheduling rules for the selected resources.

    :ivar requiredDuringSchedulingIgnoredDuringExecution: If the affinity requirements specified by
     this field are not met at scheduling time, the resource will not be scheduled onto the cluster.
     If the affinity requirements specified by this field cease to be met at some point after the
     placement (e.g. due to an update), the system may or may not try to eventually remove the
     resource from the cluster.
    :vartype requiredDuringSchedulingIgnoredDuringExecution: "ClusterSelector"
    """

    requiredDuringSchedulingIgnoredDuringExecution: "ClusterSelector"
    """If the affinity requirements specified by this field are not met at scheduling time, the
     resource will not be scheduled onto the cluster. If the affinity requirements specified by this
     field cease to be met at some point after the placement (e.g. due to an update), the system may
     or may not try to eventually remove the resource from the cluster."""


class ClusterResourcePlacementSpec(TypedDict, total=False):
    """ClusterResourcePlacementSpec defines the desired state of ClusterResourcePlacement.

    :ivar policy: Policy defines how to select member clusters to place the selected resources. If
     unspecified, all the joined member clusters are selected.
    :vartype policy: "PlacementPolicy"
    """

    policy: "PlacementPolicy"
    """Policy defines how to select member clusters to place the selected resources. If unspecified,
     all the joined member clusters are selected."""


class ClusterSelector(TypedDict, total=False):
    """ClusterSelector.

    :ivar clusterSelectorTerms: ClusterSelectorTerms is a list of cluster selector terms. The terms
     are ``ORed``. Required.
    :vartype clusterSelectorTerms: list["ClusterSelectorTerm"]
    """

    clusterSelectorTerms: Required[list["ClusterSelectorTerm"]]
    """ClusterSelectorTerms is a list of cluster selector terms. The terms are ``ORed``. Required."""


class ClusterSelectorTerm(TypedDict, total=False):
    """ClusterSelectorTerm.

    :ivar labelSelector: LabelSelector is a label query over all the joined member clusters.
     Clusters matching the query are selected. If you specify both label and property selectors in
     the same term, the results are AND'd.
    :vartype labelSelector: "LabelSelector"
    :ivar propertySelector: PropertySelector is a property query over all joined member clusters.
     Clusters matching the query are selected. If you specify both label and property selectors in
     the same term, the results are AND'd. At this moment, PropertySelector can only be used with
     ``RequiredDuringSchedulingIgnoredDuringExecution`` affinity terms. This field is beta-level; it
     is for the property-based scheduling feature and is only functional when a property provider is
     enabled in the deployment.
    :vartype propertySelector: "PropertySelector"
    """

    labelSelector: "LabelSelector"
    """LabelSelector is a label query over all the joined member clusters. Clusters matching the query
     are selected. If you specify both label and property selectors in the same term, the results
     are AND'd."""
    propertySelector: "PropertySelector"
    """PropertySelector is a property query over all joined member clusters. Clusters matching the
     query are selected. If you specify both label and property selectors in the same term, the
     results are AND'd. At this moment, PropertySelector can only be used with
     ``RequiredDuringSchedulingIgnoredDuringExecution`` affinity terms. This field is beta-level; it
     is for the property-based scheduling feature and is only functional when a property provider is
     enabled in the deployment."""


class ErrorAdditionalInfo(TypedDict, total=False):
    """The resource management error additional info.

    :ivar type: The additional info type.
    :vartype type: str
    :ivar info: The additional info.
    :vartype info: Any
    """

    type: str
    """The additional info type."""
    info: Any
    """The additional info."""


class ErrorDetail(TypedDict, total=False):
    """The error detail.

    :ivar code: The error code.
    :vartype code: str
    :ivar message: The error message.
    :vartype message: str
    :ivar target: The error target.
    :vartype target: str
    :ivar details: The error details.
    :vartype details: list["ErrorDetail"]
    :ivar additionalInfo: The error additional info.
    :vartype additionalInfo: list["ErrorAdditionalInfo"]
    """

    code: str
    """The error code."""
    message: str
    """The error message."""
    target: str
    """The error target."""
    details: list["ErrorDetail"]
    """The error details."""
    additionalInfo: list["ErrorAdditionalInfo"]
    """The error additional info."""


class TrackedResource(Resource):
    """Tracked Resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    :ivar location: The geo-location where the resource lives. Required.
    :vartype location: str
    """

    tags: dict[str, str]
    """Resource tags."""
    location: Required[str]
    """The geo-location where the resource lives. Required."""


class Fleet(TrackedResource):
    """The Fleet resource.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    :ivar location: The geo-location where the resource lives. Required.
    :vartype location: str
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: "FleetProperties"
    :ivar eTag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype eTag: str
    :ivar identity: Managed identity.
    :vartype identity: "ManagedServiceIdentity"
    """

    properties: "FleetProperties"
    """The resource-specific properties for this resource."""
    eTag: str
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""
    identity: "ManagedServiceIdentity"
    """Managed identity."""


class FleetHubProfile(TypedDict, total=False):
    """The FleetHubProfile configures the fleet hub.

    :ivar dnsPrefix: DNS prefix used to create the FQDN for the Fleet hub.
    :vartype dnsPrefix: str
    :ivar apiServerAccessProfile: The access profile for the Fleet hub API server.
    :vartype apiServerAccessProfile: "APIServerAccessProfile"
    :ivar agentProfile: The agent profile for the Fleet hub.
    :vartype agentProfile: "AgentProfile"
    :ivar fqdn: The FQDN of the Fleet hub.
    :vartype fqdn: str
    :ivar kubernetesVersion: The Kubernetes version of the Fleet hub.
    :vartype kubernetesVersion: str
    :ivar portalFqdn: The Azure Portal FQDN of the Fleet hub.
    :vartype portalFqdn: str
    """

    dnsPrefix: str
    """DNS prefix used to create the FQDN for the Fleet hub."""
    apiServerAccessProfile: "APIServerAccessProfile"
    """The access profile for the Fleet hub API server."""
    agentProfile: "AgentProfile"
    """The agent profile for the Fleet hub."""
    fqdn: str
    """The FQDN of the Fleet hub."""
    kubernetesVersion: str
    """The Kubernetes version of the Fleet hub."""
    portalFqdn: str
    """The Azure Portal FQDN of the Fleet hub."""


class FleetManagedNamespace(TrackedResource):
    """A fleet managed namespace.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    :ivar location: The geo-location where the resource lives. Required.
    :vartype location: str
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: "FleetManagedNamespaceProperties"
    :ivar eTag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype eTag: str
    """

    properties: "FleetManagedNamespaceProperties"
    """The resource-specific properties for this resource."""
    eTag: str
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""


class FleetManagedNamespacePatch(TypedDict, total=False):
    """The properties of a fleet managed namespace that can be patched.

    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    """

    tags: dict[str, str]
    """Resource tags."""


class FleetManagedNamespaceProperties(TypedDict, total=False):
    """The properties of a fleet managed namespace.

    :ivar provisioningState: The status of the last operation. Known values are: "Succeeded",
     "Failed", "Canceled", "Creating", "Updating", and "Deleting".
    :vartype provisioningState: Union[str, "FleetManagedNamespaceProvisioningState"]
    :ivar managedNamespaceProperties: The namespace properties for the fleet managed namespace.
    :vartype managedNamespaceProperties: "ManagedNamespaceProperties"
    :ivar adoptionPolicy: Action if the managed namespace with the same name already exists.
     Default is Never. Required. Known values are: "Never", "IfIdentical", and "Always".
    :vartype adoptionPolicy: Union[str, "AdoptionPolicy"]
    :ivar deletePolicy: Delete options of a fleet managed namespace. Default is Keep. Required.
     Known values are: "Keep" and "Delete".
    :vartype deletePolicy: Union[str, "DeletePolicy"]
    :ivar propagationPolicy: The profile of the propagation to create the namespace.
    :vartype propagationPolicy: "PropagationPolicy"
    :ivar status: Status information of the last operation for fleet managed namespace.
    :vartype status: "FleetManagedNamespaceStatus"
    :ivar portalFqdn: The Azure Portal FQDN of the Fleet hub.
    :vartype portalFqdn: str
    """

    provisioningState: Union[str, "FleetManagedNamespaceProvisioningState"]
    """The status of the last operation. Known values are: \"Succeeded\", \"Failed\", \"Canceled\",
     \"Creating\", \"Updating\", and \"Deleting\"."""
    managedNamespaceProperties: "ManagedNamespaceProperties"
    """The namespace properties for the fleet managed namespace."""
    adoptionPolicy: Required[Union[str, "AdoptionPolicy"]]
    """Action if the managed namespace with the same name already exists. Default is Never. Required.
     Known values are: \"Never\", \"IfIdentical\", and \"Always\"."""
    deletePolicy: Required[Union[str, "DeletePolicy"]]
    """Delete options of a fleet managed namespace. Default is Keep. Required. Known values are:
     \"Keep\" and \"Delete\"."""
    propagationPolicy: "PropagationPolicy"
    """The profile of the propagation to create the namespace."""
    status: "FleetManagedNamespaceStatus"
    """Status information of the last operation for fleet managed namespace."""
    portalFqdn: str
    """The Azure Portal FQDN of the Fleet hub."""


class FleetManagedNamespaceStatus(TypedDict, total=False):
    """Status information for the fleet managed namespace.

    :ivar lastOperationId: The last operation ID for the fleet managed namespace.
    :vartype lastOperationId: str
    :ivar lastOperationError: The last operation error of the fleet managed namespace.
    :vartype lastOperationError: "ErrorDetail"
    """

    lastOperationId: str
    """The last operation ID for the fleet managed namespace."""
    lastOperationError: "ErrorDetail"
    """The last operation error of the fleet managed namespace."""


class FleetMember(ProxyResource):
    """A member of the Fleet. It contains a reference to an existing Kubernetes cluster on Azure.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: "FleetMemberProperties"
    :ivar eTag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype eTag: str
    """

    properties: "FleetMemberProperties"
    """The resource-specific properties for this resource."""
    eTag: str
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""


class FleetMemberProperties(TypedDict, total=False):
    """A member of the Fleet. It contains a reference to an existing Kubernetes cluster on Azure.

    :ivar clusterResourceId: The ARM resource id of the cluster that joins the Fleet. Must be a
     valid Azure resource id. e.g.:
     '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ContainerService/managedClusters/{clusterName}'.
     Required.
    :vartype clusterResourceId: str
    :ivar group: The group this member belongs to for multi-cluster update management.
    :vartype group: str
    :ivar provisioningState: The status of the last operation. Known values are: "Succeeded",
     "Failed", "Canceled", "Joining", "Leaving", and "Updating".
    :vartype provisioningState: Union[str, "FleetMemberProvisioningState"]
    :ivar labels: The labels for the fleet member.
    :vartype labels: dict[str, str]
    :ivar status: Status information of the last operation for fleet member.
    :vartype status: "FleetMemberStatus"
    """

    clusterResourceId: Required[str]
    """The ARM resource id of the cluster that joins the Fleet. Must be a valid Azure resource id.
     e.g.:
     '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ContainerService/managedClusters/{clusterName}'.
     Required."""
    group: str
    """The group this member belongs to for multi-cluster update management."""
    provisioningState: Union[str, "FleetMemberProvisioningState"]
    """The status of the last operation. Known values are: \"Succeeded\", \"Failed\", \"Canceled\",
     \"Joining\", \"Leaving\", and \"Updating\"."""
    labels: dict[str, str]
    """The labels for the fleet member."""
    status: "FleetMemberStatus"
    """Status information of the last operation for fleet member."""


class FleetMemberStatus(TypedDict, total=False):
    """Status information for the fleet member.

    :ivar lastOperationId: The last operation ID for the fleet member.
    :vartype lastOperationId: str
    :ivar lastOperationError: The last operation error of the fleet member.
    :vartype lastOperationError: "ErrorDetail"
    """

    lastOperationId: str
    """The last operation ID for the fleet member."""
    lastOperationError: "ErrorDetail"
    """The last operation error of the fleet member."""


class FleetMemberUpdate(TypedDict, total=False):
    """The type used for update operations of the FleetMember.

    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: "FleetMemberUpdateProperties"
    """

    properties: "FleetMemberUpdateProperties"
    """The resource-specific properties for this resource."""


class FleetMemberUpdateProperties(TypedDict, total=False):
    """The updatable properties of the FleetMember.

    :ivar group: The group this member belongs to for multi-cluster update management.
    :vartype group: str
    :ivar labels: The labels for the fleet member.
    :vartype labels: dict[str, str]
    """

    group: str
    """The group this member belongs to for multi-cluster update management."""
    labels: dict[str, str]
    """The labels for the fleet member."""


class FleetPatch(TypedDict, total=False):
    """Properties of a Fleet that can be patched.

    :ivar tags: Resource tags.
    :vartype tags: dict[str, str]
    :ivar identity: Managed identity.
    :vartype identity: "ManagedServiceIdentity"
    """

    tags: dict[str, str]
    """Resource tags."""
    identity: "ManagedServiceIdentity"
    """Managed identity."""


class FleetProperties(TypedDict, total=False):
    """Fleet properties.

    :ivar provisioningState: The status of the last operation. Known values are: "Succeeded",
     "Failed", "Canceled", "Creating", "Updating", and "Deleting".
    :vartype provisioningState: Union[str, "FleetProvisioningState"]
    :ivar hubProfile: The FleetHubProfile configures the Fleet's hub.
    :vartype hubProfile: "FleetHubProfile"
    :ivar status: Status information for the fleet.
    :vartype status: "FleetStatus"
    """

    provisioningState: Union[str, "FleetProvisioningState"]
    """The status of the last operation. Known values are: \"Succeeded\", \"Failed\", \"Canceled\",
     \"Creating\", \"Updating\", and \"Deleting\"."""
    hubProfile: "FleetHubProfile"
    """The FleetHubProfile configures the Fleet's hub."""
    status: "FleetStatus"
    """Status information for the fleet."""


class FleetStatus(TypedDict, total=False):
    """Status information for the fleet.

    :ivar lastOperationId: The last operation ID for the fleet.
    :vartype lastOperationId: str
    :ivar lastOperationError: The last operation error for the fleet.
    :vartype lastOperationError: "ErrorDetail"
    """

    lastOperationId: str
    """The last operation ID for the fleet."""
    lastOperationError: "ErrorDetail"
    """The last operation error for the fleet."""


class FleetUpdateStrategy(ProxyResource):
    """Defines a multi-stage process to perform update operations across members of a Fleet.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: "FleetUpdateStrategyProperties"
    :ivar eTag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype eTag: str
    """

    properties: "FleetUpdateStrategyProperties"
    """The resource-specific properties for this resource."""
    eTag: str
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""


class FleetUpdateStrategyProperties(TypedDict, total=False):
    """The properties of the UpdateStrategy.

    :ivar provisioningState: The provisioning state of the UpdateStrategy resource. Known values
     are: "Succeeded", "Failed", and "Canceled".
    :vartype provisioningState: Union[str, "FleetUpdateStrategyProvisioningState"]
    :ivar strategy: Defines the update sequence of the clusters. Required.
    :vartype strategy: "UpdateRunStrategy"
    """

    provisioningState: Union[str, "FleetUpdateStrategyProvisioningState"]
    """The provisioning state of the UpdateStrategy resource. Known values are: \"Succeeded\",
     \"Failed\", and \"Canceled\"."""
    strategy: Required["UpdateRunStrategy"]
    """Defines the update sequence of the clusters. Required."""


class GateConfiguration(TypedDict, total=False):
    """GateConfiguration is used to define where Gates should be placed within the Update Run.

    :ivar displayName: The human-readable display name of the Gate.
    :vartype displayName: str
    :ivar type: The type of the Gate determines how it is completed. Required. "Approval"
    :vartype type: Union[str, "GateType"]
    """

    displayName: str
    """The human-readable display name of the Gate."""
    type: Required[Union[str, "GateType"]]
    """The type of the Gate determines how it is completed. Required. \"Approval\""""


class GatePatch(TypedDict, total=False):
    """Patch a Gate resource.

    :ivar properties: Properties of a Gate that can be patched. Required.
    :vartype properties: "GatePatchProperties"
    """

    properties: Required["GatePatchProperties"]
    """Properties of a Gate that can be patched. Required."""


class GatePatchProperties(TypedDict, total=False):
    """Properties of a Gate that can be patched.

    :ivar state: The state of the Gate. Required. Known values are: "Pending", "Skipped", and
     "Completed".
    :vartype state: Union[str, "GateState"]
    """

    state: Required[Union[str, "GateState"]]
    """The state of the Gate. Required. Known values are: \"Pending\", \"Skipped\", and \"Completed\"."""


class LabelSelector(TypedDict, total=False):
    """A label selector is a label query over a set of resources. The result of matchLabels and
    matchExpressions are ANDed. An empty label selector matches all objects. A null label selector
    matches no objects.

    :ivar matchLabels: matchLabels is a map of {key,value} pairs. A single {key,value} in the
     matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the
     operator is "In", and the values array contains only "value". The requirements are ANDed.
    :vartype matchLabels: dict[str, str]
    :ivar matchExpressions: matchExpressions is a list of label selector requirements. The
     requirements are ANDed.
    :vartype matchExpressions: list["LabelSelectorRequirement"]
    """

    matchLabels: dict[str, str]
    """matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is
     equivalent to an element of matchExpressions, whose key field is \"key\", the operator is
     \"In\", and the values array contains only \"value\". The requirements are ANDed."""
    matchExpressions: list["LabelSelectorRequirement"]
    """matchExpressions is a list of label selector requirements. The requirements are ANDed."""


class LabelSelectorRequirement(TypedDict, total=False):
    """A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    :ivar key: key is the label key that the selector applies to. Required.
    :vartype key: str
    :ivar operator: operator represents a key's relationship to a set of values. Valid operators
     are In, NotIn, Exists and DoesNotExist. Required. Known values are: "In", "NotIn", "Exists",
     and "DoesNotExist".
    :vartype operator: Union[str, "LabelSelectorOperator"]
    :ivar values: values is an array of string values. If the operator is In or NotIn, the values
     array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be
     empty. This array is replaced during a strategic merge patch.
    :vartype values: list[str]
    """

    key: Required[str]
    """key is the label key that the selector applies to. Required."""
    operator: Required[Union[str, "LabelSelectorOperator"]]
    """operator represents a key's relationship to a set of values. Valid operators are In, NotIn,
     Exists and DoesNotExist. Required. Known values are: \"In\", \"NotIn\", \"Exists\", and
     \"DoesNotExist\"."""
    values: list[str]
    """values is an array of string values. If the operator is In or NotIn, the values array must be
     non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This
     array is replaced during a strategic merge patch."""


class ManagedClusterUpdate(TypedDict, total=False):
    """The update to be applied to the ManagedClusters.

    :ivar upgrade: The upgrade to apply to the ManagedClusters. Required.
    :vartype upgrade: "ManagedClusterUpgradeSpec"
    :ivar nodeImageSelection: The node image upgrade to be applied to the target nodes in update
     run.
    :vartype nodeImageSelection: "NodeImageSelection"
    """

    upgrade: Required["ManagedClusterUpgradeSpec"]
    """The upgrade to apply to the ManagedClusters. Required."""
    nodeImageSelection: "NodeImageSelection"
    """The node image upgrade to be applied to the target nodes in update run."""


class ManagedClusterUpgradeSpec(TypedDict, total=False):
    """The upgrade to apply to a ManagedCluster.

    :ivar type: ManagedClusterUpgradeType is the type of upgrade to be applied. Required. Known
     values are: "Full", "NodeImageOnly", and "ControlPlaneOnly".
    :vartype type: Union[str, "ManagedClusterUpgradeType"]
    :ivar kubernetesVersion: The Kubernetes version to upgrade the member clusters to.
    :vartype kubernetesVersion: str
    """

    type: Required[Union[str, "ManagedClusterUpgradeType"]]
    """ManagedClusterUpgradeType is the type of upgrade to be applied. Required. Known values are:
     \"Full\", \"NodeImageOnly\", and \"ControlPlaneOnly\"."""
    kubernetesVersion: str
    """The Kubernetes version to upgrade the member clusters to."""


class ManagedNamespaceProperties(TypedDict, total=False):
    """The namespace properties for the fleet managed namespace.

    :ivar labels: The labels for the fleet managed namespace.
    :vartype labels: dict[str, str]
    :ivar annotations: The annotations for the fleet managed namespace.
    :vartype annotations: dict[str, str]
    :ivar defaultResourceQuota: The default resource quota for the fleet managed namespace.
    :vartype defaultResourceQuota: "ResourceQuota"
    :ivar defaultNetworkPolicy: The default network policy for the fleet managed namespace.
    :vartype defaultNetworkPolicy: "NetworkPolicy"
    """

    labels: dict[str, str]
    """The labels for the fleet managed namespace."""
    annotations: dict[str, str]
    """The annotations for the fleet managed namespace."""
    defaultResourceQuota: "ResourceQuota"
    """The default resource quota for the fleet managed namespace."""
    defaultNetworkPolicy: "NetworkPolicy"
    """The default network policy for the fleet managed namespace."""


class ManagedServiceIdentity(TypedDict, total=False):
    """Managed service identity (system assigned and/or user assigned identities).

    :ivar principalId: The service principal ID of the system assigned identity. This property will
     only be provided for a system assigned identity.
    :vartype principalId: str
    :ivar tenantId: The tenant ID of the system assigned identity. This property will only be
     provided for a system assigned identity.
    :vartype tenantId: str
    :ivar type: The type of managed identity assigned to this resource. Required. Known values are:
     "None", "SystemAssigned", "UserAssigned", and "SystemAssigned, UserAssigned".
    :vartype type: Union[str, "ManagedServiceIdentityType"]
    :ivar userAssignedIdentities: The identities assigned to this resource by the user.
    :vartype userAssignedIdentities: dict[str, "UserAssignedIdentity"]
    """

    principalId: str
    """The service principal ID of the system assigned identity. This property will only be provided
     for a system assigned identity."""
    tenantId: str
    """The tenant ID of the system assigned identity. This property will only be provided for a system
     assigned identity."""
    type: Required[Union[str, "ManagedServiceIdentityType"]]
    """The type of managed identity assigned to this resource. Required. Known values are: \"None\",
     \"SystemAssigned\", \"UserAssigned\", and \"SystemAssigned, UserAssigned\"."""
    userAssignedIdentities: dict[str, "UserAssignedIdentity"]
    """The identities assigned to this resource by the user."""


class MemberUpdateStatus(TypedDict, total=False):
    """The status of a member update operation.

    :ivar status: The status of the MemberUpdate operation.
    :vartype status: "UpdateStatus"
    :ivar name: The name of the FleetMember.
    :vartype name: str
    :ivar clusterResourceId: The Azure resource id of the target Kubernetes cluster.
    :vartype clusterResourceId: str
    :ivar operationId: The operation resource id of the latest attempt to perform the operation.
    :vartype operationId: str
    :ivar message: The status message after processing the member update operation.
    :vartype message: str
    """

    status: "UpdateStatus"
    """The status of the MemberUpdate operation."""
    name: str
    """The name of the FleetMember."""
    clusterResourceId: str
    """The Azure resource id of the target Kubernetes cluster."""
    operationId: str
    """The operation resource id of the latest attempt to perform the operation."""
    message: str
    """The status message after processing the member update operation."""


class NetworkPolicy(TypedDict, total=False):
    """The network policy for the managed namespace.

    :ivar ingress: The ingress policy for the managed namespace. Known values are: "AllowAll",
     "DenyAll", and "AllowSameNamespace".
    :vartype ingress: Union[str, "PolicyRule"]
    :ivar egress: The egress policy for the managed namespace. Known values are: "AllowAll",
     "DenyAll", and "AllowSameNamespace".
    :vartype egress: Union[str, "PolicyRule"]
    """

    ingress: Union[str, "PolicyRule"]
    """The ingress policy for the managed namespace. Known values are: \"AllowAll\", \"DenyAll\", and
     \"AllowSameNamespace\"."""
    egress: Union[str, "PolicyRule"]
    """The egress policy for the managed namespace. Known values are: \"AllowAll\", \"DenyAll\", and
     \"AllowSameNamespace\"."""


class NodeImageSelection(TypedDict, total=False):
    """The node image upgrade to be applied to the target nodes in update run.

    :ivar type: The node image upgrade type. Required. Known values are: "Latest", "Consistent",
     and "Custom".
    :vartype type: Union[str, "NodeImageSelectionType"]
    :ivar customNodeImageVersions: Custom node image versions to upgrade the nodes to. This field
     is required if node image selection type is Custom. Otherwise, it must be empty. For each node
     image family (e.g., 'AKSUbuntu-1804gen2containerd'), this field can contain at most one version
     (e.g., only one of 'AKSUbuntu-1804gen2containerd-2023.01.12' or
     'AKSUbuntu-1804gen2containerd-2023.02.12', not both). If the nodes belong to a family without a
     matching image version in this field, they are not upgraded.
    :vartype customNodeImageVersions: list["NodeImageVersion"]
    """

    type: Required[Union[str, "NodeImageSelectionType"]]
    """The node image upgrade type. Required. Known values are: \"Latest\", \"Consistent\", and
     \"Custom\"."""
    customNodeImageVersions: list["NodeImageVersion"]
    """Custom node image versions to upgrade the nodes to. This field is required if node image
     selection type is Custom. Otherwise, it must be empty. For each node image family (e.g.,
     'AKSUbuntu-1804gen2containerd'), this field can contain at most one version (e.g., only one of
     'AKSUbuntu-1804gen2containerd-2023.01.12' or 'AKSUbuntu-1804gen2containerd-2023.02.12', not
     both). If the nodes belong to a family without a matching image version in this field, they are
     not upgraded."""


class NodeImageSelectionStatus(TypedDict, total=False):
    """The node image upgrade specs for the update run.

    :ivar selectedNodeImageVersions: The image versions to upgrade the nodes to.
    :vartype selectedNodeImageVersions: list["NodeImageVersion"]
    """

    selectedNodeImageVersions: list["NodeImageVersion"]
    """The image versions to upgrade the nodes to."""


class NodeImageVersion(TypedDict, total=False):
    """The node upgrade image version.

    :ivar version: The image version to upgrade the nodes to (e.g.,
     'AKSUbuntu-1804gen2containerd-2022.12.13').
    :vartype version: str
    """

    version: str
    """The image version to upgrade the nodes to (e.g., 'AKSUbuntu-1804gen2containerd-2022.12.13')."""


class PlacementPolicy(TypedDict, total=False):
    """PlacementPolicy contains the rules to select target member clusters to place the selected
    resources. Note that only clusters that are both joined and satisfying the rules will be
    selected. You can only specify at most one of the two fields: ClusterNames and Affinity. If
    none is specified, all the joined clusters are selected.

    :ivar placementType: Type of placement. Can be "PickAll", "PickN" or "PickFixed". Default is
     PickAll. Known values are: "PickAll" and "PickFixed".
    :vartype placementType: Union[str, "PlacementType"]
    :ivar clusterNames: ClusterNames contains a list of names of MemberCluster to place the
     selected resources. Only valid if the placement type is "PickFixed".
    :vartype clusterNames: list[str]
    :ivar affinity: Affinity contains cluster affinity scheduling rules. Defines which member
     clusters to place the selected resources. Only valid if the placement type is "PickAll" or
     "PickN".
    :vartype affinity: "Affinity"
    :ivar tolerations: If specified, the ClusterResourcePlacement's Tolerations. Tolerations cannot
     be updated or deleted. This field is beta-level and is for the taints and tolerations feature.
    :vartype tolerations: list["Toleration"]
    """

    placementType: Union[str, "PlacementType"]
    """Type of placement. Can be \"PickAll\", \"PickN\" or \"PickFixed\". Default is PickAll. Known
     values are: \"PickAll\" and \"PickFixed\"."""
    clusterNames: list[str]
    """ClusterNames contains a list of names of MemberCluster to place the selected resources. Only
     valid if the placement type is \"PickFixed\"."""
    affinity: "Affinity"
    """Affinity contains cluster affinity scheduling rules. Defines which member clusters to place the
     selected resources. Only valid if the placement type is \"PickAll\" or \"PickN\"."""
    tolerations: list["Toleration"]
    """If specified, the ClusterResourcePlacement's Tolerations. Tolerations cannot be updated or
     deleted. This field is beta-level and is for the taints and tolerations feature."""


class PlacementProfile(TypedDict, total=False):
    """The configuration profile for default ClusterResourcePlacement for placement.

    :ivar defaultClusterResourcePlacement: The default ClusterResourcePlacement policy
     configuration.
    :vartype defaultClusterResourcePlacement: "ClusterResourcePlacementSpec"
    """

    defaultClusterResourcePlacement: "ClusterResourcePlacementSpec"
    """The default ClusterResourcePlacement policy configuration."""


class PropagationPolicy(TypedDict, total=False):
    """The propagation to be used for provisioning the namespace among the fleet.

    :ivar type: The type of the policy to be used. Default is Placement. Required. "Placement"
    :vartype type: Union[str, "PropagationType"]
    :ivar placementProfile: The profile to be used for propagation via placement.
    :vartype placementProfile: "PlacementProfile"
    """

    type: Required[Union[str, "PropagationType"]]
    """The type of the policy to be used. Default is Placement. Required. \"Placement\""""
    placementProfile: "PlacementProfile"
    """The profile to be used for propagation via placement."""


class PropertySelector(TypedDict, total=False):
    """PropertySelector helps user specify property requirements when picking clusters for resource
    placement.

    :ivar matchExpressions: MatchExpressions is an array of PropertySelectorRequirements. The
     requirements are AND'd. Required.
    :vartype matchExpressions: list["PropertySelectorRequirement"]
    """

    matchExpressions: Required[list["PropertySelectorRequirement"]]
    """MatchExpressions is an array of PropertySelectorRequirements. The requirements are AND'd.
     Required."""


class PropertySelectorRequirement(TypedDict, total=False):
    """PropertySelectorRequirement is a specific property requirement when picking clusters for
    resource placement.

    :ivar name: Name is the name of the property; it should be a Kubernetes label name. Required.
    :vartype name: str
    :ivar operator: Operator specifies the relationship between a cluster's observed value of the
     specified property and the values given in the requirement. Required. Known values are: "Gt",
     "Ge", "Eq", "Ne", "Lt", and "Le".
    :vartype operator: Union[str, "PropertySelectorOperator"]
    :ivar values: Values are a list of values of the specified property which Fleet will compare
     against the observed values of individual member clusters in accordance with the given
     operator. At this moment, each value should be a Kubernetes quantity. For more information, see
     `https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity
     <https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity>`_. If the operator is Gt
     (greater than), Ge (greater than or equal to), Lt (less than), or ``Le`` (less than or equal
     to), Eq (equal to), or Ne (ne), exactly one value must be specified in the list. Required.
    :vartype values: list[str]
    """

    name: Required[str]
    """Name is the name of the property; it should be a Kubernetes label name. Required."""
    operator: Required[Union[str, "PropertySelectorOperator"]]
    """Operator specifies the relationship between a cluster's observed value of the specified
     property and the values given in the requirement. Required. Known values are: \"Gt\", \"Ge\",
     \"Eq\", \"Ne\", \"Lt\", and \"Le\"."""
    values: Required[list[str]]
    """Values are a list of values of the specified property which Fleet will compare against the
     observed values of individual member clusters in accordance with the given operator. At this
     moment, each value should be a Kubernetes quantity. For more information, see
     `https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity
     <https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity>`_. If the operator is Gt
     (greater than), Ge (greater than or equal to), Lt (less than), or ``Le`` (less than or equal
     to), Eq (equal to), or Ne (ne), exactly one value must be specified in the list. Required."""


class ResourceQuota(TypedDict, total=False):
    """The resource quota for the managed namespace.

    :ivar cpuRequest: The CPU request for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu>`_.
    :vartype cpuRequest: str
    :ivar cpuLimit: The CPU limit for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu>`_.
    :vartype cpuLimit: str
    :ivar memoryRequest: The memory request for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory>`_.
    :vartype memoryRequest: str
    :ivar memoryLimit: The memory limit for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory>`_.
    :vartype memoryLimit: str
    """

    cpuRequest: str
    """The CPU request for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu>`_."""
    cpuLimit: str
    """The CPU limit for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-cpu>`_."""
    memoryRequest: str
    """The memory request for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory>`_."""
    memoryLimit: str
    """The memory limit for the managed namespace. See more at
     `https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory
     <https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#meaning-of-memory>`_."""


class SkipProperties(TypedDict, total=False):
    """The properties of a skip operation containing multiple skip requests.

    :ivar targets: The targets to skip. Required.
    :vartype targets: list["SkipTarget"]
    """

    targets: Required[list["SkipTarget"]]
    """The targets to skip. Required."""


class SkipTarget(TypedDict, total=False):
    """The definition of a single skip request.

    :ivar type: The skip target type. Required. Known values are: "Member", "Group", "Stage", and
     "AfterStageWait".
    :vartype type: Union[str, "TargetType"]
    :ivar name: The skip target's name. To skip a member/group/stage, use the member/group/stage's
     name; Tp skip an after stage wait, use the parent stage's name. Required.
    :vartype name: str
    """

    type: Required[Union[str, "TargetType"]]
    """The skip target type. Required. Known values are: \"Member\", \"Group\", \"Stage\", and
     \"AfterStageWait\"."""
    name: Required[str]
    """The skip target's name. To skip a member/group/stage, use the member/group/stage's name; Tp
     skip an after stage wait, use the parent stage's name. Required."""


class SystemData(TypedDict, total=False):
    """Metadata pertaining to creation and last modification of the resource.

    :ivar createdBy: The identity that created the resource.
    :vartype createdBy: str
    :ivar createdByType: The type of identity that created the resource. Known values are: "User",
     "Application", "ManagedIdentity", and "Key".
    :vartype createdByType: Union[str, "CreatedByType"]
    :ivar createdAt: The timestamp of resource creation (UTC).
    :vartype createdAt: str
    :ivar lastModifiedBy: The identity that last modified the resource.
    :vartype lastModifiedBy: str
    :ivar lastModifiedByType: The type of identity that last modified the resource. Known values
     are: "User", "Application", "ManagedIdentity", and "Key".
    :vartype lastModifiedByType: Union[str, "CreatedByType"]
    :ivar lastModifiedAt: The timestamp of resource last modification (UTC).
    :vartype lastModifiedAt: str
    """

    createdBy: str
    """The identity that created the resource."""
    createdByType: Union[str, "CreatedByType"]
    """The type of identity that created the resource. Known values are: \"User\", \"Application\",
     \"ManagedIdentity\", and \"Key\"."""
    createdAt: str
    """The timestamp of resource creation (UTC)."""
    lastModifiedBy: str
    """The identity that last modified the resource."""
    lastModifiedByType: Union[str, "CreatedByType"]
    """The type of identity that last modified the resource. Known values are: \"User\",
     \"Application\", \"ManagedIdentity\", and \"Key\"."""
    lastModifiedAt: str
    """The timestamp of resource last modification (UTC)."""


class Toleration(TypedDict, total=False):
    """Toleration allows ClusterResourcePlacement to tolerate any taint that matches the triple
    <key,value,effect> using the matching operator <operator>.

    :ivar key: Key is the taint key that the toleration applies to. Empty means match all taint
     keys. If the key is empty, operator must be Exists; this combination means to match all values
     and all keys.
    :vartype key: str
    :ivar operator: Operator represents a key's relationship to the value. Valid operators are
     Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a
     ClusterResourcePlacement can tolerate all taints of a particular category. Known values are:
     "Exists" and "Equal".
    :vartype operator: Union[str, "TolerationOperator"]
    :ivar value: Value is the taint value the toleration matches to. If the operator is Exists, the
     value should be empty, otherwise just a regular string.
    :vartype value: str
    :ivar effect: Effect indicates the taint effect to match. Empty means match all taint effects.
     When specified, only allowed value is NoSchedule. "NoSchedule"
    :vartype effect: Union[str, "TaintEffect"]
    """

    key: str
    """Key is the taint key that the toleration applies to. Empty means match all taint keys. If the
     key is empty, operator must be Exists; this combination means to match all values and all keys."""
    operator: Union[str, "TolerationOperator"]
    """Operator represents a key's relationship to the value. Valid operators are Exists and Equal.
     Defaults to Equal. Exists is equivalent to wildcard for value, so that a
     ClusterResourcePlacement can tolerate all taints of a particular category. Known values are:
     \"Exists\" and \"Equal\"."""
    value: str
    """Value is the taint value the toleration matches to. If the operator is Exists, the value should
     be empty, otherwise just a regular string."""
    effect: Union[str, "TaintEffect"]
    """Effect indicates the taint effect to match. Empty means match all taint effects. When
     specified, only allowed value is NoSchedule. \"NoSchedule\""""


class UpdateGroup(TypedDict, total=False):
    """A group to be updated.

    :ivar name: Name of the group. It must match a group name of an existing fleet member.
     Required.
    :vartype name: str
    :ivar maxConcurrency: The max number of upgrades that can run concurrently in this specific
     group. Acts as a ceiling (and not a quota) for the number of concurrent upgrades within the
     group you want to tolerate at a time. Actual concurrency may be lower depending on stage-level
     concurrency limits or individual member conditions. Group maxConcurrency has a min value of
     "1". The max value is min(number of clusters in the group, the stage maxConcurrency). If no
     value is provided, defaults to 1. Accepts either: • A fixed count, e.g. "3" • A percentage,
     e.g. "25%" (range 1–100). Percentage is of the number of clusters in the group. Fractional
     results are rounded down. A minimum of 1 upgrade is enforced. Examples: • "3" --> up to 3
     members from this group upgrade at once. • "100%" --> “all at once”, up to all members for this
     group upgrade at the same time. • "25%" --> up to 25% of the members in the group will be
     upgraded at the same time.
    :vartype maxConcurrency: str
    :ivar beforeGates: A list of Gates that will be created before this Group is executed.
    :vartype beforeGates: list["GateConfiguration"]
    :ivar afterGates: A list of Gates that will be created after this Group is executed.
    :vartype afterGates: list["GateConfiguration"]
    """

    name: Required[str]
    """Name of the group. It must match a group name of an existing fleet member. Required."""
    maxConcurrency: str
    """The max number of upgrades that can run concurrently in this specific group. Acts as a ceiling
     (and not a quota) for the number of concurrent upgrades within the group you want to tolerate
     at a time. Actual concurrency may be lower depending on stage-level concurrency limits or
     individual member conditions. Group maxConcurrency has a min value of \"1\". The max value is
     min(number of clusters in the group, the stage maxConcurrency). If no value is provided,
     defaults to 1. Accepts either: • A fixed count, e.g. \"3\" • A percentage, e.g. \"25%\" (range
     1–100). Percentage is of the number of clusters in the group. Fractional results are rounded
     down. A minimum of 1 upgrade is enforced. Examples: • \"3\" --> up to 3 members from this group
     upgrade at once. • \"100%\" --> “all at once”, up to all members for this group upgrade at the
     same time. • \"25%\" --> up to 25% of the members in the group will be upgraded at the same
     time."""
    beforeGates: list["GateConfiguration"]
    """A list of Gates that will be created before this Group is executed."""
    afterGates: list["GateConfiguration"]
    """A list of Gates that will be created after this Group is executed."""


class UpdateGroupStatus(TypedDict, total=False):
    """The status of a UpdateGroup.

    :ivar status: The status of the UpdateGroup.
    :vartype status: "UpdateStatus"
    :ivar name: The name of the UpdateGroup.
    :vartype name: str
    :ivar maxConcurrency:   The max number of upgrades that can run concurrently in this group,
     resolved from the UpdateStrategy.UpdateGroup.maxConcurrency value. If no value was provided,
     this value defaults to "1".
    :vartype maxConcurrency: int
    :ivar members: The list of member this UpdateGroup updates.
    :vartype members: list["MemberUpdateStatus"]
    :ivar beforeGates: The list of Gates that will run before this UpdateGroup.
    :vartype beforeGates: list["UpdateRunGateStatus"]
    :ivar afterGates: The list of Gates that will run after this UpdateGroup.
    :vartype afterGates: list["UpdateRunGateStatus"]
    """

    status: "UpdateStatus"
    """The status of the UpdateGroup."""
    name: str
    """The name of the UpdateGroup."""
    maxConcurrency: int
    """The max number of upgrades that can run concurrently in this group, resolved from the
     UpdateStrategy.UpdateGroup.maxConcurrency value. If no value was provided, this value defaults
     to \"1\"."""
    members: list["MemberUpdateStatus"]
    """The list of member this UpdateGroup updates."""
    beforeGates: list["UpdateRunGateStatus"]
    """The list of Gates that will run before this UpdateGroup."""
    afterGates: list["UpdateRunGateStatus"]
    """The list of Gates that will run after this UpdateGroup."""


class UpdateRun(ProxyResource):
    """A multi-stage process to perform update operations across members of a Fleet.

    :ivar id: Fully qualified resource ID for the resource. Ex -
     /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
    :vartype id: str
    :ivar name: The name of the resource.
    :vartype name: str
    :ivar type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or
     "Microsoft.Storage/storageAccounts".
    :vartype type: str
    :ivar systemData: Azure Resource Manager metadata containing createdBy and modifiedBy
     information.
    :vartype systemData: "SystemData"
    :ivar properties: The resource-specific properties for this resource.
    :vartype properties: "UpdateRunProperties"
    :ivar eTag: If eTag is provided in the response body, it may also be provided as a header per
     the normal etag convention.  Entity tags are used for comparing two or more entities from the
     same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match
     (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    :vartype eTag: str
    """

    properties: "UpdateRunProperties"
    """The resource-specific properties for this resource."""
    eTag: str
    """If eTag is provided in the response body, it may also be provided as a header per the normal
     etag convention.  Entity tags are used for comparing two or more entities from the same
     requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section
     14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields."""


class UpdateRunGateStatus(TypedDict, total=False):
    """The status of the Gate, as represented in the Update Run.

    :ivar displayName: The human-readable display name of the Gate.
    :vartype displayName: str
    :ivar gateId: The resource id of the Gate.
    :vartype gateId: str
    :ivar status: The status of the Gate.
    :vartype status: "UpdateStatus"
    """

    displayName: str
    """The human-readable display name of the Gate."""
    gateId: str
    """The resource id of the Gate."""
    status: "UpdateStatus"
    """The status of the Gate."""


class UpdateRunProperties(TypedDict, total=False):
    """The properties of the UpdateRun.

    :ivar provisioningState: The provisioning state of the UpdateRun resource. Known values are:
     "Succeeded", "Failed", and "Canceled".
    :vartype provisioningState: Union[str, "UpdateRunProvisioningState"]
    :ivar updateStrategyId: The resource id of the FleetUpdateStrategy resource to reference.

     When creating a new run, there are three ways to define a strategy for the run:

     1. Define a new strategy in place: Set the "strategy" field.
     2. Use an existing strategy: Set the "updateStrategyId" field. (since 2023-08-15-preview)
     3. Use the default strategy to update all the members one by one: Leave both "updateStrategyId"
     and "strategy" unset. (since 2023-08-15-preview)

     Setting both "updateStrategyId" and "strategy" is invalid.

     UpdateRuns created by "updateStrategyId" snapshot the referenced UpdateStrategy at the time of
     creation and store it in the "strategy" field.
     Subsequent changes to the referenced FleetUpdateStrategy resource do not propagate.
     UpdateRunStrategy changes can be made directly on the "strategy" field before launching the
     UpdateRun.
    :vartype updateStrategyId: str
    :ivar strategy: The strategy defines the order in which the clusters will be updated. If not
     set, all members will be updated sequentially. The UpdateRun status will show a single
     UpdateStage and a single UpdateGroup targeting all members. The strategy of the UpdateRun can
     be modified until the run is started.
    :vartype strategy: "UpdateRunStrategy"
    :ivar managedClusterUpdate: The update to be applied to all clusters in the UpdateRun. The
     managedClusterUpdate can be modified until the run is started. Required.
    :vartype managedClusterUpdate: "ManagedClusterUpdate"
    :ivar status: The status of the UpdateRun.
    :vartype status: "UpdateRunStatus"
    :ivar autoUpgradeProfileId: AutoUpgradeProfileId is the id of an auto upgrade profile resource.
    :vartype autoUpgradeProfileId: str
    """

    provisioningState: Union[str, "UpdateRunProvisioningState"]
    """The provisioning state of the UpdateRun resource. Known values are: \"Succeeded\", \"Failed\",
     and \"Canceled\"."""
    updateStrategyId: str
    """The resource id of the FleetUpdateStrategy resource to reference.
     
     When creating a new run, there are three ways to define a strategy for the run:
     
     1. Define a new strategy in place: Set the \"strategy\" field.
     2. Use an existing strategy: Set the \"updateStrategyId\" field. (since 2023-08-15-preview)
     3. Use the default strategy to update all the members one by one: Leave both
     \"updateStrategyId\" and \"strategy\" unset. (since 2023-08-15-preview)
     
     Setting both \"updateStrategyId\" and \"strategy\" is invalid.
     
     UpdateRuns created by \"updateStrategyId\" snapshot the referenced UpdateStrategy at the time
     of creation and store it in the \"strategy\" field.
     Subsequent changes to the referenced FleetUpdateStrategy resource do not propagate.
     UpdateRunStrategy changes can be made directly on the \"strategy\" field before launching the
     UpdateRun."""
    strategy: "UpdateRunStrategy"
    """The strategy defines the order in which the clusters will be updated. If not set, all members
     will be updated sequentially. The UpdateRun status will show a single UpdateStage and a single
     UpdateGroup targeting all members. The strategy of the UpdateRun can be modified until the run
     is started."""
    managedClusterUpdate: Required["ManagedClusterUpdate"]
    """The update to be applied to all clusters in the UpdateRun. The managedClusterUpdate can be
     modified until the run is started. Required."""
    status: "UpdateRunStatus"
    """The status of the UpdateRun."""
    autoUpgradeProfileId: str
    """AutoUpgradeProfileId is the id of an auto upgrade profile resource."""


class UpdateRunStatus(TypedDict, total=False):
    """The status of a UpdateRun.

    :ivar status: The status of the UpdateRun.
    :vartype status: "UpdateStatus"
    :ivar stages: The stages composing an update run. Stages are run sequentially withing an
     UpdateRun.
    :vartype stages: list["UpdateStageStatus"]
    :ivar nodeImageSelection: The node image upgrade specs for the update run. It is only set in
     update run when ``NodeImageSelection.type`` is ``Consistent``.
    :vartype nodeImageSelection: "NodeImageSelectionStatus"
    """

    status: "UpdateStatus"
    """The status of the UpdateRun."""
    stages: list["UpdateStageStatus"]
    """The stages composing an update run. Stages are run sequentially withing an UpdateRun."""
    nodeImageSelection: "NodeImageSelectionStatus"
    """The node image upgrade specs for the update run. It is only set in update run when
     ``NodeImageSelection.type`` is ``Consistent``."""


class UpdateRunStrategy(TypedDict, total=False):
    """Defines the update sequence of the clusters via stages and groups.

    Stages within a run are executed sequentially one after another.
    Groups within a stage are executed in parallel.
    Member clusters within a group are updated sequentially one after another.

    A valid strategy contains no duplicate groups within or across stages.

    :ivar stages: The list of stages that compose this update run. Min size: 1. Required.
    :vartype stages: list["UpdateStage"]
    """

    stages: Required[list["UpdateStage"]]
    """The list of stages that compose this update run. Min size: 1. Required."""


class UpdateStage(TypedDict, total=False):
    """Defines a stage which contains the groups to update and the steps to take (e.g., wait for a
    time period) before starting the next stage.

    :ivar name: The name of the stage. Must be unique within the UpdateRun. Required.
    :vartype name: str
    :ivar groups: Defines the groups to be executed in parallel in this stage. Duplicate groups are
     not allowed. Min size: 1.
    :vartype groups: list["UpdateGroup"]
    :ivar afterStageWaitInSeconds: The time in seconds to wait at the end of this stage before
     starting the next one. Defaults to 0 seconds if unspecified.
    :vartype afterStageWaitInSeconds: int
    :ivar maxConcurrency: The max number of upgrades that can run concurrently across all groups in
     this stage. Acts as a ceiling (and not a quota) for the number of concurrent upgrades within
     the stage you want to tolerate at a time. Actual concurrency may be lower depending on
     group-level concurrency limits or individual member conditions. Stage maxConcurrency has a min
     value of "1". Accepts either: • A fixed count, e.g., "3" • A percentage, e.g., "25%" (range
     1–100). Percentage is of the total number of clusters across all groups in the stage.
     Fractional results are rounded down. A minimum of 1 upgrade is enforced. Examples: • "3"
     --> up to 3 clusters from this stage upgrade at once (across all groups). • "100%"  --> “all at
     once”; up to all clusters in this stage upgrade at the same time. • "25%"   --> up to 25% of
     the stage’s total clusters upgrade at the same time.
    :vartype maxConcurrency: str
    :ivar beforeGates: A list of Gates that will be created before this Stage is executed.
    :vartype beforeGates: list["GateConfiguration"]
    :ivar afterGates: A list of Gates that will be created after this Stage is executed.
    :vartype afterGates: list["GateConfiguration"]
    """

    name: Required[str]
    """The name of the stage. Must be unique within the UpdateRun. Required."""
    groups: list["UpdateGroup"]
    """Defines the groups to be executed in parallel in this stage. Duplicate groups are not allowed.
     Min size: 1."""
    afterStageWaitInSeconds: int
    """The time in seconds to wait at the end of this stage before starting the next one. Defaults to
     0 seconds if unspecified."""
    maxConcurrency: str
    """The max number of upgrades that can run concurrently across all groups in this stage. Acts as a
     ceiling (and not a quota) for the number of concurrent upgrades within the stage you want to
     tolerate at a time. Actual concurrency may be lower depending on group-level concurrency limits
     or individual member conditions. Stage maxConcurrency has a min value of \"1\". Accepts either:
     • A fixed count, e.g., \"3\" • A percentage, e.g., \"25%\" (range 1–100). Percentage is of the
     total number of clusters across all groups in the stage. Fractional results are rounded down. A
     minimum of 1 upgrade is enforced. Examples: • \"3\"     --> up to 3 clusters from this stage
     upgrade at once (across all groups). • \"100%\"  --> “all at once”; up to all clusters in this
     stage upgrade at the same time. • \"25%\"   --> up to 25% of the stage’s total clusters upgrade
     at the same time."""
    beforeGates: list["GateConfiguration"]
    """A list of Gates that will be created before this Stage is executed."""
    afterGates: list["GateConfiguration"]
    """A list of Gates that will be created after this Stage is executed."""


class UpdateStageStatus(TypedDict, total=False):
    """The status of a UpdateStage.

    :ivar status: The status of the UpdateStage.
    :vartype status: "UpdateStatus"
    :ivar name: The name of the UpdateStage.
    :vartype name: str
    :ivar maxConcurrency: The max number of upgrades that can run concurrently across all groups in
     this stage, resolved from the UpdateStrategy.UpdateStage.maxConcurrency value.
    :vartype maxConcurrency: int
    :ivar groups: The list of groups to be updated as part of this UpdateStage.
    :vartype groups: list["UpdateGroupStatus"]
    :ivar beforeGates: The list of Gates that will run before this UpdateStage.
    :vartype beforeGates: list["UpdateRunGateStatus"]
    :ivar afterGates: The list of Gates that will run after this UpdateStage.
    :vartype afterGates: list["UpdateRunGateStatus"]
    :ivar afterStageWaitStatus: The status of the wait period configured on the UpdateStage.
    :vartype afterStageWaitStatus: "WaitStatus"
    """

    status: "UpdateStatus"
    """The status of the UpdateStage."""
    name: str
    """The name of the UpdateStage."""
    maxConcurrency: int
    """The max number of upgrades that can run concurrently across all groups in this stage, resolved
     from the UpdateStrategy.UpdateStage.maxConcurrency value."""
    groups: list["UpdateGroupStatus"]
    """The list of groups to be updated as part of this UpdateStage."""
    beforeGates: list["UpdateRunGateStatus"]
    """The list of Gates that will run before this UpdateStage."""
    afterGates: list["UpdateRunGateStatus"]
    """The list of Gates that will run after this UpdateStage."""
    afterStageWaitStatus: "WaitStatus"
    """The status of the wait period configured on the UpdateStage."""


class UpdateStatus(TypedDict, total=False):
    """The status for an operation or group of operations.

    :ivar startTime: The time the operation or group was started.
    :vartype startTime: str
    :ivar completedTime: The time the operation or group was completed.
    :vartype completedTime: str
    :ivar state: The State of the operation or group. Known values are: "NotStarted", "Running",
     "Stopping", "Stopped", "Skipped", "Failed", "Pending", and "Completed".
    :vartype state: Union[str, "UpdateState"]
    :ivar error: The error details when a failure is encountered.
    :vartype error: "ErrorDetail"
    """

    startTime: str
    """The time the operation or group was started."""
    completedTime: str
    """The time the operation or group was completed."""
    state: Union[str, "UpdateState"]
    """The State of the operation or group. Known values are: \"NotStarted\", \"Running\",
     \"Stopping\", \"Stopped\", \"Skipped\", \"Failed\", \"Pending\", and \"Completed\"."""
    error: "ErrorDetail"
    """The error details when a failure is encountered."""


class UserAssignedIdentity(TypedDict, total=False):
    """User assigned identity properties.

    :ivar principalId: The principal ID of the assigned identity.
    :vartype principalId: str
    :ivar clientId: The client ID of the assigned identity.
    :vartype clientId: str
    """

    principalId: str
    """The principal ID of the assigned identity."""
    clientId: str
    """The client ID of the assigned identity."""


class WaitStatus(TypedDict, total=False):
    """The status of the wait duration.

    :ivar status: The status of the wait duration.
    :vartype status: "UpdateStatus"
    :ivar waitDurationInSeconds: The wait duration configured in seconds.
    :vartype waitDurationInSeconds: int
    """

    status: "UpdateStatus"
    """The status of the wait duration."""
    waitDurationInSeconds: int
    """The wait duration configured in seconds."""
