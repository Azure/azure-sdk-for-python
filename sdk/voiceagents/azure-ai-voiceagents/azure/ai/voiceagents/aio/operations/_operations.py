# pylint: disable=too-many-lines
# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
# Code generated by Microsoft (R) Python Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is regenerated.
# --------------------------------------------------------------------------
from collections.abc import MutableMapping
from io import IOBase
import json
from typing import Any, AsyncIterator, Callable, IO, Literal, Optional, TYPE_CHECKING, TypeVar, Union, overload

from azure.core import AsyncPipelineClient
from azure.core.async_paging import AsyncItemPaged, AsyncList
from azure.core.exceptions import (
    ClientAuthenticationError,
    HttpResponseError,
    ResourceExistsError,
    ResourceNotFoundError,
    ResourceNotModifiedError,
    StreamClosedError,
    StreamConsumedError,
    map_error,
)
from azure.core.pipeline import PipelineResponse
from azure.core.rest import AsyncHttpResponse, HttpRequest
from azure.core.tracing.decorator import distributed_trace
from azure.core.tracing.decorator_async import distributed_trace_async
from azure.core.utils import case_insensitive_dict

from ... import models as _models, types as _types
from ..._utils.model_base import SdkJSONEncoder, _deserialize, _failsafe_deserialize
from ..._utils.serialization import Deserializer, Serializer
from ...models._enums import AgentDefinitionOptInKeys
from ...operations._operations import (
    build_agent_endpoint_conversations_delete_agent_conversation_request,
    build_agent_endpoint_conversations_get_agent_conversation_audio_content_request,
    build_agent_endpoint_conversations_get_agent_conversation_audio_request,
    build_agent_endpoint_conversations_get_agent_conversation_item_audio_content_request,
    build_agent_endpoint_conversations_get_agent_conversation_item_audio_request,
    build_agent_endpoint_conversations_get_agent_conversation_item_request,
    build_agent_endpoint_conversations_get_agent_conversation_request,
    build_agent_endpoint_conversations_get_agent_conversation_response_request,
    build_agent_endpoint_conversations_list_agent_conversation_items_request,
    build_agent_endpoint_conversations_list_agent_conversation_response_items_request,
    build_agent_endpoint_conversations_list_agent_conversation_responses_request,
    build_voice_agents_create_voice_agent_request,
    build_voice_agents_create_voice_agent_version_request,
    build_voice_agents_delete_voice_agent_request,
    build_voice_agents_delete_voice_agent_version_request,
    build_voice_agents_disable_voice_agent_request,
    build_voice_agents_enable_voice_agent_request,
    build_voice_agents_generate_voice_agent_request,
    build_voice_agents_get_voice_agent_request,
    build_voice_agents_get_voice_agent_version_request,
    build_voice_agents_list_voice_agent_versions_request,
    build_voice_agents_list_voice_agents_request,
    build_voice_agents_update_voice_agent_request,
)
from .._configuration import VoiceAgentsClientConfiguration

if TYPE_CHECKING:
    from ... import _unions
T = TypeVar("T")
ClsType = Optional[Callable[[PipelineResponse[HttpRequest, AsyncHttpResponse], T, dict[str, Any]], Any]]
JSON = MutableMapping[str, Any]
_Unset: Any = object()


class AgentEndpointConversationsOperations:  # pylint: disable=docstring-missing-param
    """
    .. warning::
        **DO NOT** instantiate this class directly.

        Instead, you should access the following operations through
        :class:`~azure.ai.voiceagents.aio.VoiceAgentsClient`'s
        :attr:`agent_endpoint_conversations` attribute.
    """

    def __init__(self, *args, **kwargs) -> None:
        input_args = list(args)
        self._client: AsyncPipelineClient = input_args.pop(0) if input_args else kwargs.pop("client")
        self._config: VoiceAgentsClientConfiguration = input_args.pop(0) if input_args else kwargs.pop("config")
        self._serialize: Serializer = input_args.pop(0) if input_args else kwargs.pop("serializer")
        self._deserialize: Deserializer = input_args.pop(0) if input_args else kwargs.pop("deserializer")

    @distributed_trace_async
    async def get_agent_conversation(
        self,
        agent_name: str,
        conversation_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> _models.VoiceConversation:
        """Get a voice agent conversation.

        Retrieves a single conversation recorded for the specified voice agent endpoint by its id.
        Returns ``404`` when the conversation was not persisted (``store = false``) or does not exist.

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation to retrieve. Required.
        :type conversation_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: VoiceConversation. The VoiceConversation is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceConversation
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.VoiceConversation] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_get_agent_conversation_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceConversation, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def delete_agent_conversation(
        self,
        agent_name: str,
        conversation_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> None:
        """Delete a voice agent conversation.

        Deletes a conversation and all of its stored data — responses, items, and any audio (cascade).
        This is the customer's explicit data-deletion control for voice conversations.

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation to delete. Required.
        :type conversation_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: None
        :rtype: None
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[None] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_delete_agent_conversation_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _stream = False
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [204]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if cls:
            return cls(pipeline_response, None, {})  # type: ignore

    @distributed_trace
    def list_agent_conversation_responses(
        self,
        agent_name: str,
        conversation_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        limit: Optional[int] = None,
        order: Optional[Union[str, _models.PageOrder]] = None,
        before: Optional[str] = None,
        **kwargs: Any
    ) -> AsyncItemPaged["_models.VoiceResponse"]:
        """List responses in a voice agent conversation.

        Returns a paged collection of the responses (model inference turns) recorded for the specified
        conversation. The per-response ``output`` projection may be omitted here; use the
        response-items route for the canonical paged output. Returns ``404`` when the conversation was
        not persisted (``store = false``).

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation whose responses are listed. Required.
        :type conversation_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword limit: A limit on the number of objects to be returned. Limit can range between 1 and
         100, and the
         default is 20. Default value is None.
        :paramtype limit: int
        :keyword order: Sort order by the ``created_at`` timestamp of the objects. ``asc`` for
         ascending order and``desc``
         for descending order. Known values are: "asc" and "desc". Default value is None.
        :paramtype order: str or ~azure.ai.voiceagents.models.PageOrder
        :keyword before: A cursor for use in pagination. ``before`` is an object ID that defines your
         place in the list.
         For instance, if you make a list request and receive 100 objects, ending with obj_foo, your
         subsequent call can include before=obj_foo in order to fetch the previous page of the list.
         Default value is None.
        :paramtype before: str
        :return: An iterator like instance of VoiceResponse
        :rtype: ~azure.core.async_paging.AsyncItemPaged[~azure.ai.voiceagents.models.VoiceResponse]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[list[_models.VoiceResponse]] = kwargs.pop("cls", None)

        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        def prepare_request(_continuation_token=None):

            _request = build_agent_endpoint_conversations_list_agent_conversation_responses_request(
                agent_name=agent_name,
                conversation_id=conversation_id,
                foundry_features=foundry_features,
                limit=limit,
                order=order,
                after=_continuation_token,
                before=before,
                api_version=self._config.api_version,
                headers=_headers,
                params=_params,
            )
            path_format_arguments = {
                "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
            }
            _request.url = self._client.format_url(_request.url, **path_format_arguments)
            return _request

        async def extract_data(pipeline_response):
            deserialized = pipeline_response.http_response.json()
            list_of_elem = _deserialize(
                list[_models.VoiceResponse],
                deserialized.get("data", []),
            )
            if cls:
                list_of_elem = cls(list_of_elem)  # type: ignore
            return deserialized.get("last_id") or None, AsyncList(list_of_elem)

        async def get_next(_continuation_token=None):
            _request = prepare_request(_continuation_token)

            _stream = False
            pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
                _request, stream=_stream, **kwargs
            )
            response = pipeline_response.http_response

            if response.status_code not in [200]:
                map_error(status_code=response.status_code, response=response, error_map=error_map)
                error = _failsafe_deserialize(
                    _models.ApiErrorResponse,
                    response,
                )
                raise HttpResponseError(response=response, model=error)

            return pipeline_response

        return AsyncItemPaged(get_next, extract_data)

    @distributed_trace_async
    async def get_agent_conversation_response(
        self,
        agent_name: str,
        conversation_id: str,
        response_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> _models.VoiceResponse:
        """Get a voice agent conversation response.

        Retrieves a single response from the specified conversation by its id, including its ``output``
        items, ``usage``, and status. Returns ``404`` when the conversation or response was not
        persisted (``store = false``).

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation that contains the response. Required.
        :type conversation_id: str
        :param response_id: The id of the response to retrieve. Required.
        :type response_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: VoiceResponse. The VoiceResponse is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.VoiceResponse] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_get_agent_conversation_response_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            response_id=response_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace
    def list_agent_conversation_response_items(
        self,
        agent_name: str,
        conversation_id: str,
        response_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        limit: Optional[int] = None,
        order: Optional[Union[str, _models.PageOrder]] = None,
        before: Optional[str] = None,
        **kwargs: Any
    ) -> AsyncItemPaged["_models.VoiceConversationItem"]:
        """List items produced by a voice agent conversation response.

        Returns a paged collection of the output items produced by a specific response (the response's
        output projection). For the complete ordered conversation history — including user input and
        client-created tool outputs — use the conversation items route instead. Returns ``404`` when
        the conversation or response was not persisted (``store = false``).

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation that contains the response. Required.
        :type conversation_id: str
        :param response_id: The id of the response whose output items are listed. Required.
        :type response_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword limit: A limit on the number of objects to be returned. Limit can range between 1 and
         100, and the
         default is 20. Default value is None.
        :paramtype limit: int
        :keyword order: Sort order by the ``created_at`` timestamp of the objects. ``asc`` for
         ascending order and``desc``
         for descending order. Known values are: "asc" and "desc". Default value is None.
        :paramtype order: str or ~azure.ai.voiceagents.models.PageOrder
        :keyword before: A cursor for use in pagination. ``before`` is an object ID that defines your
         place in the list.
         For instance, if you make a list request and receive 100 objects, ending with obj_foo, your
         subsequent call can include before=obj_foo in order to fetch the previous page of the list.
         Default value is None.
        :paramtype before: str
        :return: An iterator like instance of VoiceConversationItem
        :rtype:
         ~azure.core.async_paging.AsyncItemPaged[~azure.ai.voiceagents.models.VoiceConversationItem]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[list[_models.VoiceConversationItem]] = kwargs.pop("cls", None)

        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        def prepare_request(_continuation_token=None):

            _request = build_agent_endpoint_conversations_list_agent_conversation_response_items_request(
                agent_name=agent_name,
                conversation_id=conversation_id,
                response_id=response_id,
                foundry_features=foundry_features,
                limit=limit,
                order=order,
                after=_continuation_token,
                before=before,
                api_version=self._config.api_version,
                headers=_headers,
                params=_params,
            )
            path_format_arguments = {
                "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
            }
            _request.url = self._client.format_url(_request.url, **path_format_arguments)
            return _request

        async def extract_data(pipeline_response):
            deserialized = pipeline_response.http_response.json()
            list_of_elem = _deserialize(
                list[_models.VoiceConversationItem],
                deserialized.get("data", []),
            )
            if cls:
                list_of_elem = cls(list_of_elem)  # type: ignore
            return deserialized.get("last_id") or None, AsyncList(list_of_elem)

        async def get_next(_continuation_token=None):
            _request = prepare_request(_continuation_token)

            _stream = False
            pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
                _request, stream=_stream, **kwargs
            )
            response = pipeline_response.http_response

            if response.status_code not in [200]:
                map_error(status_code=response.status_code, response=response, error_map=error_map)
                error = _failsafe_deserialize(
                    _models.ApiErrorResponse,
                    response,
                )
                raise HttpResponseError(response=response, model=error)

            return pipeline_response

        return AsyncItemPaged(get_next, extract_data)

    @distributed_trace
    def list_agent_conversation_items(
        self,
        agent_name: str,
        conversation_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        limit: Optional[int] = None,
        order: Optional[Union[str, _models.PageOrder]] = None,
        before: Optional[str] = None,
        **kwargs: Any
    ) -> AsyncItemPaged["_models.VoiceConversationItem"]:
        """List items in a voice agent conversation.

        Returns a paged collection of items — the complete ordered conversation history, including user
        input, assistant output, and client-created tool outputs (transcripts + tool events). Returns
        ``404`` when the conversation was not persisted (``store = false``).

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation whose items are listed. Required.
        :type conversation_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword limit: A limit on the number of objects to be returned. Limit can range between 1 and
         100, and the
         default is 20. Default value is None.
        :paramtype limit: int
        :keyword order: Sort order by the ``created_at`` timestamp of the objects. ``asc`` for
         ascending order and``desc``
         for descending order. Known values are: "asc" and "desc". Default value is None.
        :paramtype order: str or ~azure.ai.voiceagents.models.PageOrder
        :keyword before: A cursor for use in pagination. ``before`` is an object ID that defines your
         place in the list.
         For instance, if you make a list request and receive 100 objects, ending with obj_foo, your
         subsequent call can include before=obj_foo in order to fetch the previous page of the list.
         Default value is None.
        :paramtype before: str
        :return: An iterator like instance of VoiceConversationItem
        :rtype:
         ~azure.core.async_paging.AsyncItemPaged[~azure.ai.voiceagents.models.VoiceConversationItem]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[list[_models.VoiceConversationItem]] = kwargs.pop("cls", None)

        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        def prepare_request(_continuation_token=None):

            _request = build_agent_endpoint_conversations_list_agent_conversation_items_request(
                agent_name=agent_name,
                conversation_id=conversation_id,
                foundry_features=foundry_features,
                limit=limit,
                order=order,
                after=_continuation_token,
                before=before,
                api_version=self._config.api_version,
                headers=_headers,
                params=_params,
            )
            path_format_arguments = {
                "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
            }
            _request.url = self._client.format_url(_request.url, **path_format_arguments)
            return _request

        async def extract_data(pipeline_response):
            deserialized = pipeline_response.http_response.json()
            list_of_elem = _deserialize(
                list[_models.VoiceConversationItem],
                deserialized.get("data", []),
            )
            if cls:
                list_of_elem = cls(list_of_elem)  # type: ignore
            return deserialized.get("last_id") or None, AsyncList(list_of_elem)

        async def get_next(_continuation_token=None):
            _request = prepare_request(_continuation_token)

            _stream = False
            pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
                _request, stream=_stream, **kwargs
            )
            response = pipeline_response.http_response

            if response.status_code not in [200]:
                map_error(status_code=response.status_code, response=response, error_map=error_map)
                error = _failsafe_deserialize(
                    _models.ApiErrorResponse,
                    response,
                )
                raise HttpResponseError(response=response, model=error)

            return pipeline_response

        return AsyncItemPaged(get_next, extract_data)

    @distributed_trace_async
    async def get_agent_conversation_item(
        self,
        agent_name: str,
        conversation_id: str,
        item_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> _models.VoiceConversationItem:
        """Get a voice agent conversation item.

        Retrieves a single item from the specified conversation by its id, including its transcript. An
        ``input_audio``/``output_audio`` content part indicates that audio is available for the item;
        the canonical per-item audio metadata is the ``/items/{item_id}/audio`` resource, and the bytes
        are streamed by ``/items/{item_id}/audio/content``. Returns ``404`` when the conversation or
        item was not persisted (``store = false``).

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation that contains the item. Required.
        :type conversation_id: str
        :param item_id: The id of the conversation item to retrieve. Required.
        :type item_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: VoiceConversationItem. The VoiceConversationItem is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceConversationItem
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.VoiceConversationItem] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_get_agent_conversation_item_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            item_id=item_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceConversationItem, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_agent_conversation_item_audio(
        self,
        agent_name: str,
        conversation_id: str,
        item_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> _models.VoiceItemAudioResponse:
        """Get a voice agent conversation item's audio metadata.

        Returns metadata for a single conversation item's audio segment, including the common playback
        facts (role, format/codec, sample rate, channels, offset, duration) for both Foundry-managed
        and bring-your-own-storage (BYOS) recordings; for BYOS the response additionally includes
        ``blob_uri``, the URI of the recording in the customer's own storage (no SAS) that the customer
        downloads with their own credentials. Requires the conversation to have persisted audio
        (``store = true``); returns ``404`` when the conversation, item, or its audio was not
        persisted.

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation that contains the item. Required.
        :type conversation_id: str
        :param item_id: The id of the conversation item whose audio metadata is retrieved. Required.
        :type item_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: VoiceItemAudioResponse. The VoiceItemAudioResponse is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceItemAudioResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.VoiceItemAudioResponse] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_get_agent_conversation_item_audio_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            item_id=item_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceItemAudioResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_agent_conversation_item_audio_content(  # pylint: disable=name-too-long
        self,
        agent_name: str,
        conversation_id: str,
        item_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> AsyncIterator[bytes]:
        """Stream a voice agent conversation item's audio.

        Streams a single conversation item's audio as a WAV (``audio/wav``) byte stream through the
        service (no SAS URL). This route serves Foundry-managed storage only. For
        bring-your-own-storage (BYOS) recordings the bytes are not proxied — the caller must download
        directly from customer storage using the ``blob_uri`` returned by the item's ``/audio``
        metadata route — so this route returns ``409 Conflict`` for BYOS recordings. Returns ``404``
        when the conversation, item, or its audio was not persisted (``store = false``).

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation that contains the item. Required.
        :type conversation_id: str
        :param item_id: The id of the conversation item whose audio is streamed. Required.
        :type item_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: AsyncIterator[bytes]
        :rtype: AsyncIterator[bytes]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[AsyncIterator[bytes]] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_get_agent_conversation_item_audio_content_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            item_id=item_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", True)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        response_headers = {}
        response_headers["Content-Type"] = self._deserialize("str", response.headers.get("Content-Type"))

        deserialized = response.iter_bytes() if _decompress else response.iter_raw()

        if cls:
            return cls(pipeline_response, deserialized, response_headers)  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_agent_conversation_audio(
        self,
        agent_name: str,
        conversation_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> _models.VoiceRecordingResponse:
        """Get a voice agent conversation's merged recording metadata.

        Returns metadata for the whole-call merged stereo recording (user audio on the left channel,
        agent audio on the right). The common metadata (format, sample rate, channels, channel layout,
        duration) is returned for both Foundry-managed and bring-your-own-storage (BYOS) recordings;
        for BYOS the response additionally includes ``blob_uri``, the URI of the recording in the
        customer's own storage (no SAS) that the customer downloads with their own credentials. The
        recording is built once from the per-turn segments after the session ends; a request against an
        in-progress session returns ``409``. Requires the conversation to have persisted audio (``store
        = true``); otherwise returns ``404``.

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation whose merged recording metadata is
         retrieved. Required.
        :type conversation_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: VoiceRecordingResponse. The VoiceRecordingResponse is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceRecordingResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.VoiceRecordingResponse] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_get_agent_conversation_audio_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceRecordingResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_agent_conversation_audio_content(
        self,
        agent_name: str,
        conversation_id: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> AsyncIterator[bytes]:
        """Stream a voice agent conversation's merged recording.

        Streams the whole-call merged stereo recording as a WAV (``audio/wav``) byte stream through the
        service (no SAS URL). This route serves Foundry-managed storage only. For
        bring-your-own-storage (BYOS) recordings the bytes are not proxied — the caller must download
        directly from customer storage using the ``blob_uri`` returned by the metadata route — so this
        route returns ``409 Conflict`` for BYOS recordings. A request against an in-progress session
        also returns ``409`` (a distinct condition: session-not-ended versus BYOS-download-required). A
        conversation without persisted audio (``store = false``) returns ``404``.

        :param agent_name: The name of the agent. Required.
        :type agent_name: str
        :param conversation_id: The id of the conversation whose merged recording is streamed.
         Required.
        :type conversation_id: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: AsyncIterator[bytes]
        :rtype: AsyncIterator[bytes]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[AsyncIterator[bytes]] = kwargs.pop("cls", None)

        _request = build_agent_endpoint_conversations_get_agent_conversation_audio_content_request(
            agent_name=agent_name,
            conversation_id=conversation_id,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", True)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        response_headers = {}
        response_headers["Content-Type"] = self._deserialize("str", response.headers.get("Content-Type"))

        deserialized = response.iter_bytes() if _decompress else response.iter_raw()

        if cls:
            return cls(pipeline_response, deserialized, response_headers)  # type: ignore

        return deserialized  # type: ignore


class VoiceAgentsOperations:  # pylint: disable=docstring-missing-param
    """
    .. warning::
        **DO NOT** instantiate this class directly.

        Instead, you should access the following operations through
        :class:`~azure.ai.voiceagents.aio.VoiceAgentsClient`'s
        :attr:`voice_agents` attribute.
    """

    def __init__(self, *args, **kwargs) -> None:
        input_args = list(args)
        self._client: AsyncPipelineClient = input_args.pop(0) if input_args else kwargs.pop("client")
        self._config: VoiceAgentsClientConfiguration = input_args.pop(0) if input_args else kwargs.pop("config")
        self._serialize: Serializer = input_args.pop(0) if input_args else kwargs.pop("serializer")
        self._deserialize: Deserializer = input_args.pop(0) if input_args else kwargs.pop("deserializer")

    @overload
    async def create_voice_agent(
        self,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        name: str,
        definition: _models.VoiceAgentDefinition,
        content_type: str = "application/json",
        state: Optional[Union[str, _models.AgentState]] = None,
        metadata: Optional[dict[str, str]] = None,
        description: Optional[str] = None,
        blueprint_reference: Optional[_models.AgentBlueprintReference] = None,
        draft: Optional[bool] = None,
        agent_endpoint: Optional[_models.AgentEndpointConfig] = None,
        agent_card: Optional[_models.AgentCard] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Create a voice agent.

        Creates a new voice agent, or a new version of an existing one.

        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword name: The unique name that identifies the agent. Name can be used to
         retrieve/update/delete the agent.

         * Must start and end with alphanumeric characters,
         * Can contain hyphens in the middle
         * Must not exceed 63 characters. Required.
        :paramtype name: str
        :keyword definition: The voice agent definition. Required.
        :paramtype definition: ~azure.ai.voiceagents.models.VoiceAgentDefinition
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :keyword state: The initial operational state of the agent. Defaults to 'enabled' if not
         specified. Known values are: "enabled" and "disabled". Default value is None.
        :paramtype state: str or ~azure.ai.voiceagents.models.AgentState
        :keyword metadata: Set of 16 key-value pairs that can be attached to an object. This can be
         useful for storing additional information about the object in a structured
         format, and querying for objects via API or the dashboard.

         Keys are strings with a maximum length of 64 characters. Values are strings
         with a maximum length of 512 characters. Default value is None.
        :paramtype metadata: dict[str, str]
        :keyword description: A human-readable description of the agent. Default value is None.
        :paramtype description: str
        :keyword blueprint_reference: The blueprint reference for the agent. Default value is None.
        :paramtype blueprint_reference: ~azure.ai.voiceagents.models.AgentBlueprintReference
        :keyword draft: (Preview) Whether this agent version is a draft (candidate) rather than a
         release. The service defaults to ``false`` if a value is not specified by the caller. Draft
         versions are recorded but excluded from default 'latest' resolution and are not auto-promoted.
         Default value is None.
        :paramtype draft: bool
        :keyword agent_endpoint: An optional endpoint configuration. If not specified, a default
         endpoint configuration will be set for the agent. Default value is None.
        :paramtype agent_endpoint: ~azure.ai.voiceagents.models.AgentEndpointConfig
        :keyword agent_card: Optional agent card for the agent. Default value is None.
        :paramtype agent_card: ~azure.ai.voiceagents.models.AgentCard
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def create_voice_agent(
        self,
        body: _types.CreateVoiceAgentRequest,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Create a voice agent.

        Creates a new voice agent, or a new version of an existing one.

        :param body: Required.
        :type body: ~azure.ai.voiceagents.types.CreateVoiceAgentRequest
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def create_voice_agent(
        self,
        body: IO[bytes],
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Create a voice agent.

        Creates a new voice agent, or a new version of an existing one.

        :param body: Required.
        :type body: IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace_async
    async def create_voice_agent(  # pylint: disable=too-many-locals
        self,
        body: Union[JSON, _types.CreateVoiceAgentRequest, IO[bytes]] = _Unset,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        name: str = _Unset,
        definition: _models.VoiceAgentDefinition = _Unset,
        state: Optional[Union[str, _models.AgentState]] = None,
        metadata: Optional[dict[str, str]] = None,
        description: Optional[str] = None,
        blueprint_reference: Optional[_models.AgentBlueprintReference] = None,
        draft: Optional[bool] = None,
        agent_endpoint: Optional[_models.AgentEndpointConfig] = None,
        agent_card: Optional[_models.AgentCard] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Create a voice agent.

        Creates a new voice agent, or a new version of an existing one.

        :param body: Is one of the following types: JSON, CreateVoiceAgentRequest, IO[bytes] Required.
        :type body: JSON or ~azure.ai.voiceagents.types.CreateVoiceAgentRequest or IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword name: The unique name that identifies the agent. Name can be used to
         retrieve/update/delete the agent.

         * Must start and end with alphanumeric characters,
         * Can contain hyphens in the middle
         * Must not exceed 63 characters. Required.
        :paramtype name: str
        :keyword definition: The voice agent definition. Required.
        :paramtype definition: ~azure.ai.voiceagents.models.VoiceAgentDefinition
        :keyword state: The initial operational state of the agent. Defaults to 'enabled' if not
         specified. Known values are: "enabled" and "disabled". Default value is None.
        :paramtype state: str or ~azure.ai.voiceagents.models.AgentState
        :keyword metadata: Set of 16 key-value pairs that can be attached to an object. This can be
         useful for storing additional information about the object in a structured
         format, and querying for objects via API or the dashboard.

         Keys are strings with a maximum length of 64 characters. Values are strings
         with a maximum length of 512 characters. Default value is None.
        :paramtype metadata: dict[str, str]
        :keyword description: A human-readable description of the agent. Default value is None.
        :paramtype description: str
        :keyword blueprint_reference: The blueprint reference for the agent. Default value is None.
        :paramtype blueprint_reference: ~azure.ai.voiceagents.models.AgentBlueprintReference
        :keyword draft: (Preview) Whether this agent version is a draft (candidate) rather than a
         release. The service defaults to ``false`` if a value is not specified by the caller. Draft
         versions are recorded but excluded from default 'latest' resolution and are not auto-promoted.
         Default value is None.
        :paramtype draft: bool
        :keyword agent_endpoint: An optional endpoint configuration. If not specified, a default
         endpoint configuration will be set for the agent. Default value is None.
        :paramtype agent_endpoint: ~azure.ai.voiceagents.models.AgentEndpointConfig
        :keyword agent_card: Optional agent card for the agent. Default value is None.
        :paramtype agent_card: ~azure.ai.voiceagents.models.AgentCard
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.VoiceAgentObject] = kwargs.pop("cls", None)

        if body is _Unset:
            if name is _Unset:
                raise TypeError("missing required argument: name")
            if definition is _Unset:
                raise TypeError("missing required argument: definition")
            body = {
                "agent_card": agent_card,
                "agent_endpoint": agent_endpoint,
                "blueprint_reference": blueprint_reference,
                "definition": definition,
                "description": description,
                "draft": draft,
                "metadata": metadata,
                "name": name,
                "state": state,
            }
            body = {k: v for k, v in body.items() if v is not None}
        content_type = content_type or "application/json"
        _content = None
        if isinstance(body, (IOBase, bytes)):
            _content = body
        else:
            _content = json.dumps(body, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_voice_agents_create_voice_agent_request(
            foundry_features=foundry_features,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [201]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceAgentObject, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace
    def list_voice_agents(
        self,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        limit: Optional[int] = None,
        order: Optional[Union[str, _models.PageOrder]] = None,
        before: Optional[str] = None,
        **kwargs: Any
    ) -> AsyncItemPaged["_models.VoiceAgentObject"]:
        """List voice agents.

        Returns a paged collection of voice agents.

        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword limit: A limit on the number of objects to be returned. Limit can range between 1 and
         100, and the
         default is 20. Default value is None.
        :paramtype limit: int
        :keyword order: Sort order by the ``created_at`` timestamp of the objects. ``asc`` for
         ascending order and``desc``
         for descending order. Known values are: "asc" and "desc". Default value is None.
        :paramtype order: str or ~azure.ai.voiceagents.models.PageOrder
        :keyword before: A cursor for use in pagination. ``before`` is an object ID that defines your
         place in the list.
         For instance, if you make a list request and receive 100 objects, ending with obj_foo, your
         subsequent call can include before=obj_foo in order to fetch the previous page of the list.
         Default value is None.
        :paramtype before: str
        :return: An iterator like instance of VoiceAgentObject
        :rtype: ~azure.core.async_paging.AsyncItemPaged[~azure.ai.voiceagents.models.VoiceAgentObject]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[list[_models.VoiceAgentObject]] = kwargs.pop("cls", None)

        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        def prepare_request(_continuation_token=None):

            _request = build_voice_agents_list_voice_agents_request(
                foundry_features=foundry_features,
                limit=limit,
                order=order,
                after=_continuation_token,
                before=before,
                api_version=self._config.api_version,
                headers=_headers,
                params=_params,
            )
            path_format_arguments = {
                "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
            }
            _request.url = self._client.format_url(_request.url, **path_format_arguments)
            return _request

        async def extract_data(pipeline_response):
            deserialized = pipeline_response.http_response.json()
            list_of_elem = _deserialize(
                list[_models.VoiceAgentObject],
                deserialized.get("data", []),
            )
            if cls:
                list_of_elem = cls(list_of_elem)  # type: ignore
            return deserialized.get("last_id") or None, AsyncList(list_of_elem)

        async def get_next(_continuation_token=None):
            _request = prepare_request(_continuation_token)

            _stream = False
            pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
                _request, stream=_stream, **kwargs
            )
            response = pipeline_response.http_response

            if response.status_code not in [200]:
                map_error(status_code=response.status_code, response=response, error_map=error_map)
                error = _failsafe_deserialize(
                    _models.ApiErrorResponse,
                    response,
                )
                raise HttpResponseError(response=response, model=error)

            return pipeline_response

        return AsyncItemPaged(get_next, extract_data)

    @distributed_trace_async
    async def get_voice_agent(
        self,
        agent_name: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Get a voice agent.

        Retrieves a voice agent by its unique name.

        :param agent_name: The name of the voice agent to retrieve. Required.
        :type agent_name: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.VoiceAgentObject] = kwargs.pop("cls", None)

        _request = build_voice_agents_get_voice_agent_request(
            agent_name=agent_name,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceAgentObject, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @overload
    async def update_voice_agent(
        self,
        agent_name: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        definition: _models.VoiceAgentDefinition,
        content_type: str = "application/json",
        metadata: Optional[dict[str, str]] = None,
        description: Optional[str] = None,
        blueprint_reference: Optional[_models.AgentBlueprintReference] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Update a voice agent.

        Updates a voice agent by adding a new version if there are any changes to the agent definition.
        If no changes, returns the existing agent version.

        :param agent_name: The name of the voice agent to update. Required.
        :type agent_name: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword definition: The voice agent definition. Required.
        :paramtype definition: ~azure.ai.voiceagents.models.VoiceAgentDefinition
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :keyword metadata: Set of 16 key-value pairs that can be attached to an object. This can be
         useful for storing additional information about the object in a structured
         format, and querying for objects via API or the dashboard.

         Keys are strings with a maximum length of 64 characters. Values are strings
         with a maximum length of 512 characters. Default value is None.
        :paramtype metadata: dict[str, str]
        :keyword description: A human-readable description of the agent. Default value is None.
        :paramtype description: str
        :keyword blueprint_reference: The blueprint reference for the agent. Default value is None.
        :paramtype blueprint_reference: ~azure.ai.voiceagents.models.AgentBlueprintReference
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def update_voice_agent(
        self,
        agent_name: str,
        body: _types.UpdateVoiceAgentRequest,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Update a voice agent.

        Updates a voice agent by adding a new version if there are any changes to the agent definition.
        If no changes, returns the existing agent version.

        :param agent_name: The name of the voice agent to update. Required.
        :type agent_name: str
        :param body: Required.
        :type body: ~azure.ai.voiceagents.types.UpdateVoiceAgentRequest
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def update_voice_agent(
        self,
        agent_name: str,
        body: IO[bytes],
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Update a voice agent.

        Updates a voice agent by adding a new version if there are any changes to the agent definition.
        If no changes, returns the existing agent version.

        :param agent_name: The name of the voice agent to update. Required.
        :type agent_name: str
        :param body: Required.
        :type body: IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace_async
    async def update_voice_agent(
        self,
        agent_name: str,
        body: Union[JSON, _types.UpdateVoiceAgentRequest, IO[bytes]] = _Unset,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        definition: _models.VoiceAgentDefinition = _Unset,
        metadata: Optional[dict[str, str]] = None,
        description: Optional[str] = None,
        blueprint_reference: Optional[_models.AgentBlueprintReference] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Update a voice agent.

        Updates a voice agent by adding a new version if there are any changes to the agent definition.
        If no changes, returns the existing agent version.

        :param agent_name: The name of the voice agent to update. Required.
        :type agent_name: str
        :param body: Is one of the following types: JSON, UpdateVoiceAgentRequest, IO[bytes] Required.
        :type body: JSON or ~azure.ai.voiceagents.types.UpdateVoiceAgentRequest or IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword definition: The voice agent definition. Required.
        :paramtype definition: ~azure.ai.voiceagents.models.VoiceAgentDefinition
        :keyword metadata: Set of 16 key-value pairs that can be attached to an object. This can be
         useful for storing additional information about the object in a structured
         format, and querying for objects via API or the dashboard.

         Keys are strings with a maximum length of 64 characters. Values are strings
         with a maximum length of 512 characters. Default value is None.
        :paramtype metadata: dict[str, str]
        :keyword description: A human-readable description of the agent. Default value is None.
        :paramtype description: str
        :keyword blueprint_reference: The blueprint reference for the agent. Default value is None.
        :paramtype blueprint_reference: ~azure.ai.voiceagents.models.AgentBlueprintReference
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.VoiceAgentObject] = kwargs.pop("cls", None)

        if body is _Unset:
            if definition is _Unset:
                raise TypeError("missing required argument: definition")
            body = {
                "blueprint_reference": blueprint_reference,
                "definition": definition,
                "description": description,
                "metadata": metadata,
            }
            body = {k: v for k, v in body.items() if v is not None}
        content_type = content_type or "application/json"
        _content = None
        if isinstance(body, (IOBase, bytes)):
            _content = body
        else:
            _content = json.dumps(body, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_voice_agents_update_voice_agent_request(
            agent_name=agent_name,
            foundry_features=foundry_features,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceAgentObject, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def delete_voice_agent(
        self,
        agent_name: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> None:
        """Delete a voice agent.

        Deletes a voice agent and all of its versions.

        :param agent_name: The name of the voice agent to delete. Required.
        :type agent_name: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: None
        :rtype: None
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[None] = kwargs.pop("cls", None)

        _request = build_voice_agents_delete_voice_agent_request(
            agent_name=agent_name,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _stream = False
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [204]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if cls:
            return cls(pipeline_response, None, {})  # type: ignore

    @distributed_trace_async
    async def enable_voice_agent(
        self,
        agent_name: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> None:
        """Enable a voice agent.

        Enables the specified voice agent, allowing it to accept new requests. This operation is
        idempotent — enabling an already-enabled voice agent returns success with no side effects.

        :param agent_name: The name of the voice agent to enable. Required.
        :type agent_name: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: None
        :rtype: None
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[None] = kwargs.pop("cls", None)

        _request = build_voice_agents_enable_voice_agent_request(
            agent_name=agent_name,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _stream = False
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [204]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if cls:
            return cls(pipeline_response, None, {})  # type: ignore

    @distributed_trace_async
    async def disable_voice_agent(
        self,
        agent_name: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> None:
        """Disable a voice agent.

        Disables the specified voice agent, preventing it from accepting new requests. This operation
        is idempotent — disabling an already-disabled voice agent returns success with no side effects.

        :param agent_name: The name of the voice agent to disable. Required.
        :type agent_name: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: None
        :rtype: None
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[None] = kwargs.pop("cls", None)

        _request = build_voice_agents_disable_voice_agent_request(
            agent_name=agent_name,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _stream = False
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [204]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if cls:
            return cls(pipeline_response, None, {})  # type: ignore

    @overload
    async def generate_voice_agent(
        self,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        name: str,
        model_type: Union[str, _models.VoiceModelType],
        model: str,
        agent_type: Union[str, _models.VoiceAgentType],
        use_case: Union[str, _models.VoiceAgentUseCase],
        goal: str,
        content_type: str = "application/json",
        description: Optional[str] = None,
        tools: Optional[list["_unions.VoiceAgentTool"]] = None,
        draft: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Generate a voice agent.

        Generates and creates a voice agent from high-level inputs plus a natural-language goal. The
        operation expands the goal into a full, editable definition, creates the agent through the
        standard voice create path, and returns the created ``VoiceAgentObject``. The caller can edit
        or override the generated fields afterward through normal versioning.

        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword name: The unique name for the agent to create. Required.
        :paramtype name: str
        :keyword model_type: How the model backing the generated agent is served: ``managed``
         (service-managed) or ``self_deployed`` (the customer's own deployment). Carried through to the
         generated definition, not generated. Known values are: "managed" and "self_deployed". Required.
        :paramtype model_type: str or ~azure.ai.voiceagents.models.VoiceModelType
        :keyword model: The model paired with ``model_type``: the service-managed model name when
         ``managed``, or the customer's Foundry deployment name when ``self_deployed``. Carried through,
         not generated. Required.
        :paramtype model: str
        :keyword agent_type: The persona/tone to steer generation. Known values are: "personal" and
         "business". Required.
        :paramtype agent_type: str or ~azure.ai.voiceagents.models.VoiceAgentType
        :keyword use_case: The scenario-template catalog entry the generator specializes for. Known
         values are: "customer_support", "reception", "sales", "travel_assistant", "outreach",
         "personal_assistant", "learning", "call_center", and "in_car". Required.
        :paramtype use_case: str or ~azure.ai.voiceagents.models.VoiceAgentUseCase
        :keyword goal: A natural-language description of what the agent should do; the seed for the
         generated ``instructions``. Required.
        :paramtype goal: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :keyword description: An optional description for the agent. Generated from ``goal`` when
         omitted. Default value is None.
        :paramtype description: str
        :keyword tools: Optional tools carried through verbatim onto the generated agent (see
         ``VoiceAgentTool``). Default value is None.
        :paramtype tools: list[~azure.ai.voiceagents.models.RealtimeFunctionTool or
         ~azure.ai.voiceagents.models.VoiceAgentMcpTool or ~azure.ai.voiceagents.models.VoiceSystemTool
         or ~azure.ai.voiceagents.models.VoiceToolboxTool]
        :keyword draft: (Preview) When ``true``, the generated voice agent is created as a draft — an
         editable, unpublished version the caller can review and refine before publishing it via the
         standard create/version path. The service defaults to ``false`` if a value is not specified by
         the caller, in which case the agent is created and published normally. Default value is None.
        :paramtype draft: bool
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def generate_voice_agent(
        self,
        body: _types.GenerateVoiceAgentRequest,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Generate a voice agent.

        Generates and creates a voice agent from high-level inputs plus a natural-language goal. The
        operation expands the goal into a full, editable definition, creates the agent through the
        standard voice create path, and returns the created ``VoiceAgentObject``. The caller can edit
        or override the generated fields afterward through normal versioning.

        :param body: Required.
        :type body: ~azure.ai.voiceagents.types.GenerateVoiceAgentRequest
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def generate_voice_agent(
        self,
        body: IO[bytes],
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Generate a voice agent.

        Generates and creates a voice agent from high-level inputs plus a natural-language goal. The
        operation expands the goal into a full, editable definition, creates the agent through the
        standard voice create path, and returns the created ``VoiceAgentObject``. The caller can edit
        or override the generated fields afterward through normal versioning.

        :param body: Required.
        :type body: IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace_async
    async def generate_voice_agent(  # pylint: disable=too-many-locals
        self,
        body: Union[JSON, _types.GenerateVoiceAgentRequest, IO[bytes]] = _Unset,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        name: str = _Unset,
        model_type: Union[str, _models.VoiceModelType] = _Unset,
        model: str = _Unset,
        agent_type: Union[str, _models.VoiceAgentType] = _Unset,
        use_case: Union[str, _models.VoiceAgentUseCase] = _Unset,
        goal: str = _Unset,
        description: Optional[str] = None,
        tools: Optional[list["_unions.VoiceAgentTool"]] = None,
        draft: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentObject:
        """Generate a voice agent.

        Generates and creates a voice agent from high-level inputs plus a natural-language goal. The
        operation expands the goal into a full, editable definition, creates the agent through the
        standard voice create path, and returns the created ``VoiceAgentObject``. The caller can edit
        or override the generated fields afterward through normal versioning.

        :param body: Is one of the following types: JSON, GenerateVoiceAgentRequest, IO[bytes]
         Required.
        :type body: JSON or ~azure.ai.voiceagents.types.GenerateVoiceAgentRequest or IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword name: The unique name for the agent to create. Required.
        :paramtype name: str
        :keyword model_type: How the model backing the generated agent is served: ``managed``
         (service-managed) or ``self_deployed`` (the customer's own deployment). Carried through to the
         generated definition, not generated. Known values are: "managed" and "self_deployed". Required.
        :paramtype model_type: str or ~azure.ai.voiceagents.models.VoiceModelType
        :keyword model: The model paired with ``model_type``: the service-managed model name when
         ``managed``, or the customer's Foundry deployment name when ``self_deployed``. Carried through,
         not generated. Required.
        :paramtype model: str
        :keyword agent_type: The persona/tone to steer generation. Known values are: "personal" and
         "business". Required.
        :paramtype agent_type: str or ~azure.ai.voiceagents.models.VoiceAgentType
        :keyword use_case: The scenario-template catalog entry the generator specializes for. Known
         values are: "customer_support", "reception", "sales", "travel_assistant", "outreach",
         "personal_assistant", "learning", "call_center", and "in_car". Required.
        :paramtype use_case: str or ~azure.ai.voiceagents.models.VoiceAgentUseCase
        :keyword goal: A natural-language description of what the agent should do; the seed for the
         generated ``instructions``. Required.
        :paramtype goal: str
        :keyword description: An optional description for the agent. Generated from ``goal`` when
         omitted. Default value is None.
        :paramtype description: str
        :keyword tools: Optional tools carried through verbatim onto the generated agent (see
         ``VoiceAgentTool``). Default value is None.
        :paramtype tools: list[~azure.ai.voiceagents.models.RealtimeFunctionTool or
         ~azure.ai.voiceagents.models.VoiceAgentMcpTool or ~azure.ai.voiceagents.models.VoiceSystemTool
         or ~azure.ai.voiceagents.models.VoiceToolboxTool]
        :keyword draft: (Preview) When ``true``, the generated voice agent is created as a draft — an
         editable, unpublished version the caller can review and refine before publishing it via the
         standard create/version path. The service defaults to ``false`` if a value is not specified by
         the caller, in which case the agent is created and published normally. Default value is None.
        :paramtype draft: bool
        :return: VoiceAgentObject. The VoiceAgentObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.VoiceAgentObject] = kwargs.pop("cls", None)

        if body is _Unset:
            if name is _Unset:
                raise TypeError("missing required argument: name")
            if model_type is _Unset:
                raise TypeError("missing required argument: model_type")
            if model is _Unset:
                raise TypeError("missing required argument: model")
            if agent_type is _Unset:
                raise TypeError("missing required argument: agent_type")
            if use_case is _Unset:
                raise TypeError("missing required argument: use_case")
            if goal is _Unset:
                raise TypeError("missing required argument: goal")
            body = {
                "agent_type": agent_type,
                "description": description,
                "draft": draft,
                "goal": goal,
                "model": model,
                "model_type": model_type,
                "name": name,
                "tools": tools,
                "use_case": use_case,
            }
            body = {k: v for k, v in body.items() if v is not None}
        content_type = content_type or "application/json"
        _content = None
        if isinstance(body, (IOBase, bytes)):
            _content = body
        else:
            _content = json.dumps(body, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_voice_agents_generate_voice_agent_request(
            foundry_features=foundry_features,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceAgentObject, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @overload
    async def create_voice_agent_version(
        self,
        agent_name: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        definition: _models.VoiceAgentDefinition,
        content_type: str = "application/json",
        metadata: Optional[dict[str, str]] = None,
        description: Optional[str] = None,
        blueprint_reference: Optional[_models.AgentBlueprintReference] = None,
        draft: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentVersionObject:
        """Create a voice agent version.

        Creates a new version for the specified voice agent and returns the created version resource.

        :param agent_name: The name of the voice agent. Required.
        :type agent_name: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword definition: The voice agent definition. Required.
        :paramtype definition: ~azure.ai.voiceagents.models.VoiceAgentDefinition
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :keyword metadata: Set of 16 key-value pairs that can be attached to an object. This can be
         useful for storing additional information about the object in a structured
         format, and querying for objects via API or the dashboard.

         Keys are strings with a maximum length of 64 characters. Values are strings
         with a maximum length of 512 characters. Default value is None.
        :paramtype metadata: dict[str, str]
        :keyword description: A human-readable description of the agent. Default value is None.
        :paramtype description: str
        :keyword blueprint_reference: The blueprint reference for the agent. Default value is None.
        :paramtype blueprint_reference: ~azure.ai.voiceagents.models.AgentBlueprintReference
        :keyword draft: (Preview) Whether this agent version is a draft (candidate) rather than a
         release. The service defaults to ``false`` if a value is not specified by the caller. Draft
         versions are recorded but excluded from default 'latest' resolution and are not auto-promoted.
         Default value is None.
        :paramtype draft: bool
        :return: VoiceAgentVersionObject. The VoiceAgentVersionObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentVersionObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def create_voice_agent_version(
        self,
        agent_name: str,
        body: _types.CreateVoiceAgentVersionRequest,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentVersionObject:
        """Create a voice agent version.

        Creates a new version for the specified voice agent and returns the created version resource.

        :param agent_name: The name of the voice agent. Required.
        :type agent_name: str
        :param body: Required.
        :type body: ~azure.ai.voiceagents.types.CreateVoiceAgentVersionRequest
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentVersionObject. The VoiceAgentVersionObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentVersionObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    async def create_voice_agent_version(
        self,
        agent_name: str,
        body: IO[bytes],
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.VoiceAgentVersionObject:
        """Create a voice agent version.

        Creates a new version for the specified voice agent and returns the created version resource.

        :param agent_name: The name of the voice agent. Required.
        :type agent_name: str
        :param body: Required.
        :type body: IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: VoiceAgentVersionObject. The VoiceAgentVersionObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentVersionObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace_async
    async def create_voice_agent_version(
        self,
        agent_name: str,
        body: Union[JSON, _types.CreateVoiceAgentVersionRequest, IO[bytes]] = _Unset,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        definition: _models.VoiceAgentDefinition = _Unset,
        metadata: Optional[dict[str, str]] = None,
        description: Optional[str] = None,
        blueprint_reference: Optional[_models.AgentBlueprintReference] = None,
        draft: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.VoiceAgentVersionObject:
        """Create a voice agent version.

        Creates a new version for the specified voice agent and returns the created version resource.

        :param agent_name: The name of the voice agent. Required.
        :type agent_name: str
        :param body: Is one of the following types: JSON, CreateVoiceAgentVersionRequest, IO[bytes]
         Required.
        :type body: JSON or ~azure.ai.voiceagents.types.CreateVoiceAgentVersionRequest or IO[bytes]
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword definition: The voice agent definition. Required.
        :paramtype definition: ~azure.ai.voiceagents.models.VoiceAgentDefinition
        :keyword metadata: Set of 16 key-value pairs that can be attached to an object. This can be
         useful for storing additional information about the object in a structured
         format, and querying for objects via API or the dashboard.

         Keys are strings with a maximum length of 64 characters. Values are strings
         with a maximum length of 512 characters. Default value is None.
        :paramtype metadata: dict[str, str]
        :keyword description: A human-readable description of the agent. Default value is None.
        :paramtype description: str
        :keyword blueprint_reference: The blueprint reference for the agent. Default value is None.
        :paramtype blueprint_reference: ~azure.ai.voiceagents.models.AgentBlueprintReference
        :keyword draft: (Preview) Whether this agent version is a draft (candidate) rather than a
         release. The service defaults to ``false`` if a value is not specified by the caller. Draft
         versions are recorded but excluded from default 'latest' resolution and are not auto-promoted.
         Default value is None.
        :paramtype draft: bool
        :return: VoiceAgentVersionObject. The VoiceAgentVersionObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentVersionObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.VoiceAgentVersionObject] = kwargs.pop("cls", None)

        if body is _Unset:
            if definition is _Unset:
                raise TypeError("missing required argument: definition")
            body = {
                "blueprint_reference": blueprint_reference,
                "definition": definition,
                "description": description,
                "draft": draft,
                "metadata": metadata,
            }
            body = {k: v for k, v in body.items() if v is not None}
        content_type = content_type or "application/json"
        _content = None
        if isinstance(body, (IOBase, bytes)):
            _content = body
        else:
            _content = json.dumps(body, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_voice_agents_create_voice_agent_version_request(
            agent_name=agent_name,
            foundry_features=foundry_features,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceAgentVersionObject, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace
    def list_voice_agent_versions(
        self,
        agent_name: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        limit: Optional[int] = None,
        order: Optional[Union[str, _models.PageOrder]] = None,
        before: Optional[str] = None,
        include_drafts: Optional[bool] = None,
        **kwargs: Any
    ) -> AsyncItemPaged["_models.VoiceAgentVersionObject"]:
        """List voice agent versions.

        Returns a paged collection of versions for the specified voice agent.

        :param agent_name: The name of the voice agent to retrieve versions for. Required.
        :type agent_name: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :keyword limit: A limit on the number of objects to be returned. Limit can range between 1 and
         100, and the
         default is 20. Default value is None.
        :paramtype limit: int
        :keyword order: Sort order by the ``created_at`` timestamp of the objects. ``asc`` for
         ascending order and``desc``
         for descending order. Known values are: "asc" and "desc". Default value is None.
        :paramtype order: str or ~azure.ai.voiceagents.models.PageOrder
        :keyword before: A cursor for use in pagination. ``before`` is an object ID that defines your
         place in the list.
         For instance, if you make a list request and receive 100 objects, ending with obj_foo, your
         subsequent call can include before=obj_foo in order to fetch the previous page of the list.
         Default value is None.
        :paramtype before: str
        :keyword include_drafts: (Preview) Whether to include draft versions in the listing. The
         service defaults to ``false`` if a value is not specified by the caller (only non-draft
         versions are returned). Default value is None.
        :paramtype include_drafts: bool
        :return: An iterator like instance of VoiceAgentVersionObject
        :rtype:
         ~azure.core.async_paging.AsyncItemPaged[~azure.ai.voiceagents.models.VoiceAgentVersionObject]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[list[_models.VoiceAgentVersionObject]] = kwargs.pop("cls", None)

        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        def prepare_request(_continuation_token=None):

            _request = build_voice_agents_list_voice_agent_versions_request(
                agent_name=agent_name,
                foundry_features=foundry_features,
                limit=limit,
                order=order,
                after=_continuation_token,
                before=before,
                include_drafts=include_drafts,
                api_version=self._config.api_version,
                headers=_headers,
                params=_params,
            )
            path_format_arguments = {
                "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
            }
            _request.url = self._client.format_url(_request.url, **path_format_arguments)
            return _request

        async def extract_data(pipeline_response):
            deserialized = pipeline_response.http_response.json()
            list_of_elem = _deserialize(
                list[_models.VoiceAgentVersionObject],
                deserialized.get("data", []),
            )
            if cls:
                list_of_elem = cls(list_of_elem)  # type: ignore
            return deserialized.get("last_id") or None, AsyncList(list_of_elem)

        async def get_next(_continuation_token=None):
            _request = prepare_request(_continuation_token)

            _stream = False
            pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
                _request, stream=_stream, **kwargs
            )
            response = pipeline_response.http_response

            if response.status_code not in [200]:
                map_error(status_code=response.status_code, response=response, error_map=error_map)
                error = _failsafe_deserialize(
                    _models.ApiErrorResponse,
                    response,
                )
                raise HttpResponseError(response=response, model=error)

            return pipeline_response

        return AsyncItemPaged(get_next, extract_data)

    @distributed_trace_async
    async def get_voice_agent_version(
        self,
        agent_name: str,
        agent_version: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> _models.VoiceAgentVersionObject:
        """Get a voice agent version.

        Retrieves the specified version of a voice agent by its agent name and version identifier.

        :param agent_name: The name of the voice agent to retrieve. Required.
        :type agent_name: str
        :param agent_version: The version of the voice agent to retrieve. Required.
        :type agent_version: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: VoiceAgentVersionObject. The VoiceAgentVersionObject is compatible with MutableMapping
        :rtype: ~azure.ai.voiceagents.models.VoiceAgentVersionObject
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.VoiceAgentVersionObject] = kwargs.pop("cls", None)

        _request = build_voice_agents_get_voice_agent_version_request(
            agent_name=agent_name,
            agent_version=agent_version,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.VoiceAgentVersionObject, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def delete_voice_agent_version(
        self,
        agent_name: str,
        agent_version: str,
        *,
        foundry_features: Literal[AgentDefinitionOptInKeys.VOICE_AGENTS_V1_PREVIEW],
        **kwargs: Any
    ) -> None:
        """Delete a voice agent version.

        Deletes a specific version of a voice agent.

        :param agent_name: The name of the voice agent to delete. Required.
        :type agent_name: str
        :param agent_version: The version of the voice agent to delete. Required.
        :type agent_version: str
        :keyword foundry_features: A feature flag opt-in required when using preview operations or
         modifying persisted preview resources. VOICE_AGENTS_V1_PREVIEW. Required.
        :paramtype foundry_features: str or ~azure.ai.voiceagents.models.VOICE_AGENTS_V1_PREVIEW
        :return: None
        :rtype: None
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[None] = kwargs.pop("cls", None)

        _request = build_voice_agents_delete_voice_agent_version_request(
            agent_name=agent_name,
            agent_version=agent_version,
            foundry_features=foundry_features,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _stream = False
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [204]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ApiErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if cls:
            return cls(pipeline_response, None, {})  # type: ignore
