from setuptools import setup

# Minimal stub: all project metadata and dependencies are defined in pyproject.toml.
# Retained only so legacy tools invoking `python setup.py ...` still work.
if __name__ == "__main__":
    setup()
