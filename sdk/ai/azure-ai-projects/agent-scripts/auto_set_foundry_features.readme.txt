These are the GitHub CoPilot instructions used to edit emitted code, in order to set the
required Foundry-Features HTTP request headers. Before running the Agent,
make sure your package sources code (files under \sdk\ai\azure-ai-projects\azure) do not have any
mypy errors.

The agent created the Python script "auto_set_foundry_features.py" in this folder. Next time
you re-emit from TypeSpec, try running that script from the \sdk\ai\azure-ai-projects folder
and see if again it did the job correctly.

Model used was GPT-5.3-Codex.

Last updated: 2/18/2026

-----------------------------


Write a Python script that will apply all the changes mentioned below.
Save the Python script in the folder "agent-scripts" and call it "auto_set_foundry_features.py".
The assumption is that this script runs in the folder \azure-sdk-for-python\sdk\ai\azure-ai-projects
to apply the desired changes.

The Python script is only allowed to edit these two files:
\sdk\ai\azure-ai-projects\azure\ai\projects\aio\operations\_operations.py and
\sdk\ai\azure-ai-projects\azure\ai\projects\operations\_operations.py.

Apply all the steps mentioned below to each one of those two files.

Step 1:
At the top of each Python script, you will find some comments, followed by a section of
"from" and "import" statements. After all of those, insert the following line:

_get_foundry_features_opt_in_keys: str = ",".join([key.value for key in _models.AgentDefinitionOptInKeys] + [key.value for key in _models.FoundryFeaturesOptInKeys])

Step 2:
Find all the methods that show "foundry_features" as an input argument. It could be optional or required.
You can search for the string
"foundry_features: " to find all those. But I would like you to ignore all the methods with
names that start with "build_". They should not be modified.

For each method you find, delete the "foundry_feature" line from the method signature.

Also delete the method doc strings lines associated with this (now deleted) input argument. Those will
be the lines that start with ":keyword foundry_features:" and ":paramtype foundry_features:".

Step 3:
For each one of the method you found, also look for the line:
foundry_features=foundry_features
and replace it with:
foundry_features=_get_foundry_features_opt_in_keys if (self.__class__.__name__.startswith("Beta") or self._config._allow_preview) else None,

Step 4:
For each one of the methods you found, also see if it includes a function defintion with
the following signature:
def prepare_request(next_link=None):

If so, look for the following line inside that function:
"GET", urllib.parse.urljoin(next_link, _parsed_next_link.path), params=_next_request_params

And replace it with this line:
"GET", urllib.parse.urljoin(next_link, _parsed_next_link.path), params=_next_request_params, headers={"Foundry-Features": _SERIALIZER.header("foundry_features", foundry_features, "str")}

Step 5:
After your created the Python script, run it in the folder \azure-sdk-for-python\sdk\ai\azure-ai-projects
in order to apply the changes mentioned above.

Step 6:
To verify your result, run the MyPy tool in the same folder and make sure there are no errors originating
from the package source file. To run MyPy:
tox run -e mypy -c ../../../eng/tox/tox.ini --root
there should be no errors from files under the folder \azure-sdk-for-python\sdk\ai\azure-ai-projects\azure folder.
