# Azure AI Extensions OpenAI client library for Python

This beta package centralizes OpenAI-compatible Azure AI types and lightweight
wire helpers used by Azure AI packages.

It is the only Azure AI package in this consolidation path that directly
depends on the official `openai` package.
