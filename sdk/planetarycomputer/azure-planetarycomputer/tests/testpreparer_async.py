# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
# Code generated by Microsoft (R) Python Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is regenerated.
# --------------------------------------------------------------------------
from azure.planetarycomputer.aio import (
    GeocatalogClient,
    IngestionManagementClient,
    SharedAccessSignatureClient,
    StacClient,
    TilerClient,
)
from devtools_testutils import AzureRecordedTestCase


class GeocatalogClientTestBaseAsync(AzureRecordedTestCase):

    def create_async_client(self, endpoint):
        credential = self.get_credential(GeocatalogClient, is_async=True)
        return self.create_client_from_credential(
            GeocatalogClient,
            credential=credential,
            endpoint=endpoint,
        )


class IngestionManagementClientTestBaseAsync(AzureRecordedTestCase):

    def create_async_client(self, endpoint):
        credential = self.get_credential(IngestionManagementClient, is_async=True)
        return self.create_client_from_credential(
            IngestionManagementClient,
            credential=credential,
            endpoint=endpoint,
        )


class StacClientTestBaseAsync(AzureRecordedTestCase):

    def create_async_client(self, endpoint):
        credential = self.get_credential(StacClient, is_async=True)
        return self.create_client_from_credential(
            StacClient,
            credential=credential,
            endpoint=endpoint,
        )


class TilerClientTestBaseAsync(AzureRecordedTestCase):

    def create_async_client(self, endpoint):
        credential = self.get_credential(TilerClient, is_async=True)
        return self.create_client_from_credential(
            TilerClient,
            credential=credential,
            endpoint=endpoint,
        )


class SharedAccessSignatureClientTestBaseAsync(AzureRecordedTestCase):

    def create_async_client(self, endpoint):
        credential = self.get_credential(SharedAccessSignatureClient, is_async=True)
        return self.create_client_from_credential(
            SharedAccessSignatureClient,
            credential=credential,
            endpoint=endpoint,
        )
