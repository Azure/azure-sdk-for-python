# Release History

## 0.1.0 (Unreleased)

Initial release.

### Other Changes

- Added a `_version.py` file and updated `setup.py` to read the package version from it. This unblocks the `sdk_set_dev_version` tooling, which requires a discoverable `_version.py` for every non-metapackage.
